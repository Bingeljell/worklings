; Engine configuration file.
; It's best edited using the editor UI and not directly,
; since the parameters that go here are not all obvious.
;
; Format:
;   [section] ; section goes between []
;   param=value ; assign values to parameters

config_version=5

[application]

config/name="Worklings"
; The full version INCLUDING the prerelease label, which the macOS bundle cannot
; hold: CFBundleShortVersionString allows only three integers, so the .app knows
; it is 0.1.0 build 13 and only this knows it is alpha.13. Shown at the foot of
; the character window. Bump it with the build number in export_presets.cfg.
config/version="0.1.0-alpha.14"
run/main_scene="res://scenes/desktop_pet.tscn"
config/features=PackedStringArray("4.7", "C#")
; The same art the macOS export bakes into icon.icns. Set here as well so Godot
; itself — the project list, the editor, the window — shows Worklings rather than
; the default engine icon. It does not change the Dock icon of a run from source:
; that process is Godot.app, and only an exported bundle carries our own.
config/icon="res://assets/app-icon.png"

[display]

window/size/viewport_width=1920
window/size/viewport_height=1080
window/stretch/mode="canvas_items"
window/per_pixel_transparency/allowed=true

[dotnet]

project/assembly_name="Worklings"

[rendering]

textures/vram_compression/import_etc2_astc=true
