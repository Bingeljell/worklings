# Godot's import cache and generated artefacts — regenerated on first open
.godot/
# Exported builds
build/

# Character exports are tracked now that the asset standard is locked
# (20k triangles / 1024 textures — docs/engineering/character-asset-pipeline.md).
# Three characters total 22 MB. See assets/characters/.gitignore for which
# files are derived and excluded.
