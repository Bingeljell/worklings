# Changelog

- **2026-09-03 > godot/worklings/scenes/, godot/worklings/core/stage/ActorAnimations.cs > the player steers the fight again, and the Monolith stops being a large cat > Two follow-ons to the delve wiring. **`CombatEncounter.Decide` was ported, verified, and dead**: the scene resolved every encounter to completion at the top and replayed its log, which was faithful to the rules and quietly removed the player from the fight — an encounter already resolved has no decision left to take. The encounter is now **stepped**: resolve a round, animate what it produced, and when it asks for a decision hand the pause to the player. A log cursor is the whole bookkeeping, because the log only grows, and determinism is unchanged — same seed, same decisions, same fight, with the decisions now an input. All four reasons come through in a full chain: the Flicker's **Opening**, the Monolith's **Telegraph** (brace, or eat it), a faltering pet, and the cadence beat. 1/2/3 pick the Approach, U unleashes, Space holds; the Approach carries into the next encounter, as the design has it set at prep and re-chosen here. `AutoPush` becomes `AutoPlay` since it now covers both of the player's beats. **The Monolith borrows the Pangolin's body** — a pet model, but a heavy armoured slammer with a full action set, where a Flicker scaled to 1.75 read as a large cat. The foe slot is no longer one `.glb` baked into the `.tscn`: the scene keeps every model it has and shows one at a time. **Open, and next:** measured against the design's five beats, **prep/loadout is the one that does not exist** — the beat that makes the briefing mean anything, fully supported by the core and needing only a screen; the delve's own beats still share the fight's one line of placeholder text; and none of alpha.8's dungeon audio is in Godot yet.**

- **2026-09-03 > godot/worklings/scenes/CacheWarrenScene.cs, godot/worklings/core/stage/ > the Godot scene runs a real delve, not one fight on a loop > The payoff for the port. `CacheWarrenScene` built its fighter by hand — fixed stats, one Flicker, restart forever — because nothing else existed; now a `PetState` makes the fighter through `Combatant.Pet` (gear folded in, *then* condition), `Delve` chains the four Cache Warren encounters, and the run resolves once at the end through `Delve.Resolution`, which is the only place XP, needs and gear move. The scene grows three beats around the fight it already played: a briefing, the **bank-or-push choice** (Space or B; `AutoPush` takes it automatically for an unattended run), and a closing summary naming the exit tier and the spoils. **Verified by driving the whole loop headless** with the beats compressed: four encounters, a drop from each cleared one at the right tier (Scavenged, Scavenged, Solid), a retreat at the Monolith, then a *second* delve starting from the first one's write-back — Lv 4 to Lv 5, and max HP down from 65 to 56 because a downed exit costs condition and condition scales Vitality. One bug found that way and fixed: the briefing opened on the *previous* delve's foe, so the second run showed the Monolith at 63/90 before its first Scamp appeared. **Three of the four foes have no model** and render as the Flicker's mesh at a different size and energy colour — deliberate, because the chain's pacing is judgeable now rather than after four bakes, though the boss reading as a large cat is the most obviously placeholder thing on screen. The boss also brings events the old scene never saw — `Slammed`, `Telegraphed`, `Hardened` — which now read as a wind-up and a slam rather than silence. **The pet is held in memory only**: a run's XP, gear and condition carry into the next run and are lost when the scene closes, because nothing loads or saves a `PetState` yet. **Open, and next:** the probes still print rather than assert, so the reference outputs want committing with a runner; and the delve's own beats — briefing, the press-your-luck prompt, the summary — currently share the fight's one line of placeholder text, which is a debug readout standing in for the emotional centre of a delve.**

- **2026-09-02 > godot/worklings/core/, godot/worklings/tools/, docs/engineering/godot-port-status.md > the dungeon-facing half of CompanionCore is ported > Completes the Swift→C# port for everything the dungeon depends on: **~1,190 → ~3,166 of 5,351 lines**, 22% → 59%. A Workling with a level, gear and condition can now be built, fight a four-encounter delve, bank or push deeper, take drops, and have the result written back — where yesterday the Ram fought with hardcoded stats on a single looping encounter. Landed leaf-first, because `PetState` holds a `PetStats`, a `PetClass` and a `Loadout`: PetProgression, PetFamily, Items, DailyTally, PetNeeds, PetState, the `PetState`→`Combatant` bridge, CombatRewards, Delve, CharacterSheet — one commit each, each verified before the next began. **461 reference lines across ten probes, all diffing clean** against output captured from the running Swift implementation, with coverage made exhaustive wherever a switch could be quietly wrong: every item priced against all five families, every exit-tier boundary in both outcomes, the mood ladder in precedence order, four complete delves with every intermediate HP/XP/drop, and replay determinism across four seeds. **The port's third RNG bug was found, and it had been latent since the combat slice**: `SeededGenerator.NextBelow` implemented modulo-with-rejection, where Swift uses **Lemire's multiply-shift** — the high half of the 128-bit product of word and bound, not a remainder (seed 1, bound 5: the raw word mod 5 is 0, Swift returns 2). It survived because **nothing called it** — it was written ahead of any caller and combat never draws a bounded integer, so 107 identical fight events and a green RNG probe sat on top of a broken method for the entire port. `Delve` was its first real caller, and it surfaced as four delves matching Swift on HP, XP, exit tiers and determinism while awarding completely different gear. The lesson is about the suite rather than the method: **a ported method with no caller is not verified by the suite passing.** A second language-shape bug: Swift's `PetStats()` gives every stat its starting value of 5, while C#'s implicit parameterless *struct* constructor ignores default arguments and zeroes all five (as does `default(PetStats)`) — a fresh Workling had 0 Power, built cleanly and fought happily, so `PetStats` is a class now, where a null reference throws instead. Two more cross-language traps handled: name length counts grapheme clusters to match Swift's `String.count`, and `DailyTally` compares *local* calendar days to match `Calendar.current` — a UTC comparison passes every obvious test and rolls the day over at the wrong hour. **Persistence is deliberately not ported**: `PetStateFileStore` is unported and there is no JSON layer to verify a decoder against, so the five migration rules Swift's `init(from decoder:)` holds are written into the status doc rather than lost with the decision. **Open, and next:** `CacheWarrenScene` still builds its combatants by hand — everything it needs now exists, so wiring a real `Delve` against a real `PetState` is the payoff and nothing blocks it; and the probes print rather than assert, with the reference files living outside the repo, so they verify a port once and catch no regression afterwards — exactly the failure mode the Lemire bug demonstrates.**

- **2026-09-01 > godot/worklings/, docs/engineering/godot-port-status.md > combat ported to C#, wired to the stage, and given a HUD > The day's build work after the engine decision. **The combat slice of `CompanionCore` is in C#** — SeededGenerator, StatusEffect, CombatStats, PetCombatRates, Combatant, CombatResolver, Bestiary, CombatEncounter, ~1,190 of 5,351 lines — each file verified against reference output captured from the running Swift implementation before moving on, ending with **four complete fights across all four foe archetypes producing byte-identical 107-event logs**. That verification caught two bugs nothing else would have: Swift's `Double.random(in: 0..<1)` takes the LOW 53 bits of the word (I took the high ones), and closed ranges take the HIGH bits (having learned the first, I assumed they matched — they are the opposite, and every strike draws its damage swing from a closed range). Both produced entirely plausible fights that silently diverged. **The fight is now visible**: CombatEncounter emits CombatEvents and the scene consumes them, never touching the rules — the rules stay verifiable headlessly and the renderer stays swappable. **Impact frames were built before the attackers could reach each other** — the combatants stand ~11 units apart and are ~5 units long, so attacks played in place landed nowhere near the defender and the whole reaction was firing at a collision that never happened; feedback was "it reads like two models playing animations one after another", which was the correct diagnosis. AttackLunge closes the distance, and contact now drives damage, hit-stop, shake, sparks and numbers. Camera shake was separately wrong three ways (amplitude ~20px at 1080p, displacing along world XY when the camera is angled, quadratic falloff killing the motion in exactly the frames where a shake registers). **Family colour is the stylization spine**: each of the five families already carries a signature energy in the approved character direction, so FamilyEnergy drives HP bar, damage numbers, hit spark and impact flash from one value — a crit reads differently per family with no extra art, and the Ram's Blender crackle becomes the first instance of a pattern rather than a one-off. Also: lagging health bars (the gap between the fast fill and the slow red bar IS the damage), world-space damage numbers at the victim, the anime impact frame, hit sparks replacing a brown dust puff that was blending into the floor, Chakra Petch bundled under OFL because a game cannot depend on a font server, a two-phase beat so the countdown stops draining through the attack, and a fix for the impact firing at the START of a two-second attack animation rather than its contact frame. AttackersTravel defaults to false — travelling reads as sliding, because the mesh translates while playing a stationary attack. **Ported status is now tracked in docs/engineering/godot-port-status.md** rather than reconstructed from git log; the Swift app is still the product and PetState is the largest single unblock.**

- **2026-09-01 > scripts/blender_export_character.py, docs/engineering/character-asset-pipeline.md > character asset standard locked: 20k triangles, 1024² textures, ≤44-frame actions > The Blender→Godot pipeline for every Workling and foe, with every level reviewed in Godot on the real lit stage rather than in Blender's viewport — whose flat shading hides exactly the detail loss that matters, and which had earlier led to the wrong call that 12k triangles was indistinguishable from 283k. It is not: under real lighting the Ram's fleece flattens below 20k. **Result: three characters from ~73 MB to 23 MB** (Ram 23→7.1, Flicker 10.4→3.3, Pangolin 39.3→12.6). **The least intuitive finding is that animation data, not geometry or textures, drives file size** — cost scales with frames × 283 joints, so the Pangolin's 120-frame actions cost ~12 MB against the Ram's 4 MB and kept it heavy even after its 4096 maps came down to 1024. Decimating below 20k is near-pointless by comparison (40k→5k saves 2.5 MB and visibly costs quality) where trimming the Ram's 17 actions to its real four saves ~3.6 MB for free. Textures downscale **on export, never in the source** — the .blend is the only place the original exists and downscaling is one-way. **Six silent failure modes are now handled and documented, every one of which reported success**: a hidden rig cannot be selected so `use_selection` drops it (the Flicker and Pangolin first exported as a mesh with no skeleton and no animations, while the Ram worked only because its rig happened to be visible); split vertices at UV seams block decimate collapse unless welded first; the exporter writes a skinned mesh's base topology and ignores live modifiers so decimation must be destructive; glTF flattens a curvature-driven emission mix into a uniform emissiveFactor, which turned the Ram solid blue at 3× strength; Blender is Z-up and Godot Y-up; and Godot's import cache does not always invalidate when a `.glb` changes. The script now reads the exported `.glb` back and raises if skins or animations are zero, since the exporter cannot be trusted to say so. **Open:** the 44-frame cap is probably wrong for idles — it would make a breathing loop 1.8s where the Ram's is deliberately 6s — so it likely wants splitting into two numbers rather than being applied blindly.**

- **2026-09-01 > docs/engineering/rendering-engine-fork.md, godot/worklings/ > engine decided: Godot; Cache Warren stage rebuilt there > The day's second and larger call. Three things converged: the dungeon went live 3D (unblocking the most engine-specific work in the project — per-character shaders, particles, impact frames), **SceneKit turned out to be soft-deprecated as of WWDC 2025** (critical fixes only, RealityKit named as the migration path), and Windows is a firm ship target rather than an aspiration. RealityKit and Windows are mutually exclusive since both are Apple-only, so the real fork was SceneKit-for-now versus Godot-now — and the cheapest moment to switch is before writing the effects layer, not after. **Audience data reframed the question**: the instinct that casual pet/MMO players are Windows-first is right about gaming (Steam is 93.7/2.3/4.0) but wrong for this product, whose hook is developer activity (Stack Overflow 2025 professional: 49.5 Windows / 32.9 macOS / 27.7 Linux). That does not rescue Mac-only — it condemns it harder — but it promotes **Linux from a rounding error to ~28%**, nearly level with macOS, which argues for Godot specifically as the only option reaching all three. Two corrections to earlier analysis, both mine: `CompanionCore` is portable in *design* but not in *language* — it is Swift, so it carries to RealityKit but is a rewrite under Godot, the largest single number in that column; and the rewrite cost was framed as year-scale risk, which was a human-team estimate applied to a case that does not resemble one (the core is pure, deterministic, and has a check suite acting as a correctness oracle). **Asset probe run before committing**, on the principle that a broken rig is better found early: the Ram exported to glTF and loaded into Godot 4.7.2 at the locked camera with the walk playing, and glTF beat USDZ on every axis — all 17 actions in one file versus one, correct up-axis versus a Z-up rotation fix, automatic textures versus manual unpacking, vertex colours present versus lost, correct frame ranges throughout. The walk imports at exactly 1.333s (32 frames at 24fps) with no hand-tuning. Two findings carried forward: `Crackle_Curvature` survives glTF (it was lost through USD, so the planned texture-channel bake is unnecessary — a runtime shader can read it), and Blender material node trees flatten on export under *any* engine — the crackle arrived as a uniform emissive factor glowing the whole Ram blue, and must be rebuilt as a real shader. The Cache Warren stage now exists as an editable Godot scene at the same locked camera, carrying the numbers across unchanged; **the Ram casts a real shadow there**, making `makeContactShadow`'s code-generated blurred-oval decal unnecessary. Tooling: Godot 4.7.2 (.NET) installed to /Applications, .NET SDK 10.0.400 system-wide. Character .glb files are gitignored for now — 23 MB each at full resolution, pending the decimation and animation-file pipeline.**

- **2026-09-01 > docs/design/dungeons.md, docs/design/bake-spec.md > dungeon actors move to live 3D; baked sprite billboards dropped for the dungeon > Reverses the standing call that baked sprites are the actor format everywhere in the dungeon with live 3D reserved for the character/gear screen. The reason: **billboards were a performance optimisation for a budget that isn't tight.** A dungeon holds two characters, maybe four if multiple foes or multiplayer land, and a decimation ladder at the locked camera measured 12k tris as indistinguishable from the 283k original at game size — so four actors is ~48k triangles. The costs being paid to avoid that non-cost were real: billboards draw with `.constant` lighting and ignore lights entirely, so a lit room containing an unlit flat sprite reproduces the same "pasted on" read that killed the flat-backdrop experiment, just from the other side; and they require the whole bake pipeline (per-azimuth frame sequences, per-character ground-offset fractions, a frame library reading an absolute path outside the repo that would need bundling as app resources first). The models are already rigged with walk/idle/attack/wince. Untested cost of the new path is export and animation wiring — what SceneKit can ingest (`.dae` vs `.usdz` vs GLB) with rigs and actions intact, worth proving on one character first. Baked sprites remain the format for the desktop pet, where the constraints differ; bake-spec.md is marked accordingly. Two knock-ons recorded: impact frames never depended on this either way (they need actors in the scene graph, not any particular actor format), and the crackle post-mortem's "surface-tracing effects must be baked" finding is now a question rather than a conclusion, since a runtime shader on a real mesh does have the surface a flat billboard lacked.**

- **2026-09-01 > Sources/Worklings/DungeonStage3D.swift, DungeonStageCameraTool.swift, docs/design/dungeons.md > first room-kit piece shipped; stage flattened to one floor; stage actors lifted out of the debug tool > Three things landed on `feature/dungeon-room-kit`. **The floor is now one baked tile, repeated.** The Blender blockout's cave floor was a 21,091-vert mesh carrying two Displace modifiers, and at the locked camera's 39.7° elevation that displacement never read in silhouette — 41,600 triangles spent on shading a normal map gives for free. A 200×200-segment 4×4 patch carrying the same displace recipe and material was baked to albedo + normal on a flat quad, then made tileable with a separable triangular-window cross-dissolve against the half-shifted copy (a window that vanishes at the borders and sums to 1 with its own half-shift, so the wrap is continuous by construction; measured seam delta 0.0016/0.0060). Result: **21,091 verts → 4**, no visible seam or repetition across 11×8.5 repeats. `tiledKitMaterial` is the reusable half. **The four-band blockout collapsed into one flat floor** — party floor, arena gap, raised foe platform and back wall were scaffolding from before the room was real geometry, and the Blender room they mirror is a single flat floor; both combatants now stand on the same surface, costing the two-tier depth read until the kit's platform-edge piece brings it back. **Stage actors moved out of `#if DEBUG`** into `DungeonStageSlot`/`DungeonStageActors`, shared with the real arena the way the room already is — the seam needed before arena combatants can become scene nodes, which is the actual blocker on impact frames. Also: the Blender preview camera was moved onto the locked Cache Warren camera (Blender now previews the real game shot rather than a wider, differently-angled one), and a decimation ladder measured **12k tris as indistinguishable from the 283k original** at game size. Two open questions recorded rather than silently resolved — billboards vs. live 3D actors now that the room is lit (the `.constant` billboard reproduces the "pasted on" read from the other direction), and where impact frames get built (decided: the real arena, not the tool, because the tool has no combat loop to integrate against).**

- **2026-08-28 > docs/design/dungeons.md > closed the flat-backdrop experiment; adopted a modular Blender-kit workflow for the room > The 2026-08-27 flat-painted-backdrop prototype and its follow-up gradient-light shader (below) were a genuine test against real reference art, and it failed on two counts: the "pasted on" read never fully closed (a reference screenshot showed both characters lit flat next to visibly-colored light sources they should have picked up), and every fix attempted — contact shadow position, sampled tint colors, gradient direction — was hand-tuned against one specific painted image with no way to carry forward to the next dungeon's art. Decision: the room stays real 3D geometry with real lights (the original call), built as a reusable modular kit rather than one bespoke scene per dungeon — floor/wall/corner pieces plus per-dungeon props modeled in Blender (reusing the same scripted-RPC workflow already built for character rigs), exported and imported into SceneKit, assembled with Xcode's own built-in `.scn` scene editor rather than custom tooling. A custom browser-based dungeon-builder tool (data-driven room layouts, no Swift rebuild to move a piece) is noted as a deferred idea, not started.**

- **2026-08-28 > Sources/Worklings/DungeonStageCameraTool.swift > a fake directional-light gradient for the backdrop prototype, and a collapsible controls panel > Continues the backdrop-vs-3D-room test (2026-08-27 entries below): the flat multiply tint synced to the backdrop's sampled color read as "too subtle" because it can only shift a sprite's average hue, not put a lit side and a shadow side on it — visible directly in a reference screenshot where the Ram stands right next to a warm torch glow but is lit perfectly flat, and the Flicker stands in a cyan crystal glow with no cyan pickup at all. Added a third toggle, "directional light gradient," that fakes a light direction instead of a color: a linear gradient across the billboard's own UV space (`_surface.diffuseTexcoord`, read in a `.fragment` shader modifier same as the tint), warm/bright toward a per-corner direction and cooler/dimmer away from it, with the direction and blend strength exposed as live typeable fields rather than hardcoded — the UV-space guess for which way is "toward the light" is untested against SceneKit's actual texcoord orientation, so it needs to be tunable by eye rather than re-derived by hand. Also fixed a real bug the first test run hit: the strength field had no clamp, and a value typed past 1.0 (the field's `mix` call extrapolates rather than blends past that point) washed the whole sprite out to a flat, wrong color — now clamped in the shader itself. Separately, the growing controls overlay had started covering the party billboard at this camera framing; made the whole panel collapsible behind a small persistent `▾/▸ controls` tab (also bound to the `c` key) rather than relocating it, since a fixed-position panel and a fixed character position will collide again under some other combination of params.**

- **2026-08-27 > worklings-blender-work/tempest-ram-rigify-natural-walk.blend, Sources/Worklings/DungeonStageCameraTool.swift > Tempest Ram's electrical theming, baked into the material rather than layered live > Reference art (a fully-charged concept render) made clear the ask was glowing cracks etched along the Ram's actual contours — horn ridges, wool-tuft edges, joints — not a 2D overlay flickering on top of the sprite, which was tried first and abandoned (`DungeonEffects.swift`, since deleted) because a flat billboard has no information about the mesh's real surface to trace. Rebuilt as a Blender material effect instead: a per-vertex curvature attribute (`Crackle_Curvature`, computed via bmesh vertex-normal-vs-neighbor-average, percentile-clipped to isolate real ridges from wool-bump noise) drives an emissive Mix Shader, with a `CAM_TARGET`-rotation-independent, frame-driven traveling wave (single forward sweep, ~2s pause, reverse sweep, ~2s pause, tuned to fit the idle action's 144-frame/6s loop exactly so it repeats with no jump-cut) plus ten small floating spark/arc anchors (alternating dot spheres and fractal jagged-line curves, same midpoint-displacement technique as the 2D bolt prototype) placed across back/belly/general zones — not filtered to "front-facing for today's two azimuths," since which side faces camera will change with future dungeon entry directions and a holistic effect that relies on ordinary depth occlusion outlives that. Iterated through several rounds of user feedback (too dense/"Christmas tree," too washed-out/white from `Standard` view-transform clipping, timing too fast/looping every second, needed real dead-time between passes) before landing here. Reclassifies this effect from dungeons.md's "Effects — baked vs. live" call — Tempest Ram's crackle is baked, not live — see that doc's update below. Baked into `RamIdle_Breathe_Paw` at both stage azimuths only; not yet applied to the walk/headbutt/damage actions, which would each need their own pass/pause timing re-tuned to that action's own (much shorter) frame range.**

- **2026-08-27 > Sources/Worklings/DungeonStageCameraTool.swift, Package.swift, assets/dungeons/cache-warren-v1-noChars.png > flat painted backdrop prototype, plus contact shadows > Tests dungeons.md's "no flat backdrop" call against real reference art rather than assuming it — added a toggleable backdrop mode to the camera tool that swaps the grey 3D blockout for a single flat plane textured with a painted cave reference, sized via the real locked camera's FOV so it exactly fills 1280×720 at whatever depth it's placed. Two placement bugs found and fixed by eye: the plane's position was a fixed world coordinate rather than being placed along the camera's actual (diagonal) view axis, which shifted the whole image toward one corner; and the chosen depth (22) was nearer the camera than the characters (~30), so the backdrop occluded them entirely — fixed to 36. Verdict so far: color-viable but "characters look pasted on, not from the same universe" — diagnosed as missing contact shadows, no atmospheric depth/haze falloff for the far corner, a render-fidelity mismatch (crisp CG cutouts on a soft painted image), and no directional light match, not just a color problem. Added contact shadows first (a soft dark ground decal at each fixed corner position, not parented to the billboard since its `SCNBillboardConstraint` would tilt an attached child off the ground) — first pass was invisible against the backdrop's already near-black ground (~RGB 14-28, almost no headroom to darken further), fixed with a smaller, much more opaque near-black core and a tighter falloff. Also tried a lighting-color-tint toggle (sampled actual backdrop pixel colors at each corner's ground position, applied as a per-billboard tint): `SCNMaterial.multiply.contents` turned out to be a no-op with `lightingModel = .constant` (confirmed by it visibly doing nothing), fixed by switching to a fragment shader modifier, which does apply — but the tint alone reads as too subtle to solve "pasted on," confirming it was never purely a color problem. Remaining open, not yet started: atmospheric haze/desaturation for the foe corner, edge softness/render-fidelity matching, and directional rim-light matching the backdrop's local light sources.**

- **2026-08-27 > worklings-blender-work/clockwork-pangolin-rigify.blend, Sources/Worklings/DungeonStageCameraTool.swift, docs/design/bake-spec.md > fixed the Pangolin's tail clipping the stage frame > Its `CAM_TARGET`/`QuickCam2` camera rig had been built ad hoc during the prior bake session and never saved, so reopening the file left only a bare perspective camera and no record of the ortho scale used. Rebuilt the rig and found the actual cause: the Pangolin's body is 2.22 BU nose-to-tail but only 1.11 BU tall, and the height-only auto-sizing rule (bbox height × 1.5) used for the other two characters starves the horizontal axis on a body this elongated. Measured the true worst-case extent by projecting the posed mesh into camera space across every baked action at both stage azimuths (2.59 BU, mid-swing in the tail-swipe attack) and set ortho scale to 3.0 — margin over the measured max, not a guess. Rebaked all five picked Pangolin actions at both azimuths and resaved the `.blend` with the rig included this time. Widening the scale also moved where the character's feet sit within its own frame, so `groundOffsetFraction["pangolin"]` in the camera tool was recomputed from 0.305 to 0.179 alongside it — a stale value there would have reintroduced the platform-clipping bug from the 2026-08-26 pass.**

- **2026-08-27 > Sources/Worklings/DungeonStageCameraTool.swift, docs/design/dungeons.md > re-centered the Dungeon Stage Camera Tool's default view > The tool's starting orbit target skewed the whole 16:9 frame toward the top-right, leaving a large dead zone bottom-left. Target moved from (-3.60, -0.63, 5.16) to (-1.92, -0.10, 2.29); azimuth/elevation/radius (59.7°/39.7°/27.95) unchanged.**

- **2026-08-26 > Sources/Worklings/DungeonStageCameraTool.swift, image-to-3dlab/scripts/blender_stage_bake.py, docs/design/bake-spec.md > real animated rigs baked into the stage camera tool > Tempest Ram, Forest Flicker, and Clockwork Pangolin all now carry full Rigify action sets (walk/attack/hurt-or-wince/special/idle); a new reusable render script (`blender_stage_bake.py`) bakes any of them at the locked dungeon-stage azimuths (35°/245°, bake-spec §10) without hand-written one-off RPC snippets, building the `CAM_TARGET`/ortho-camera rig from the mesh's own bounding box where a file didn't have one yet. `DungeonStageCameraTool` swapped its single frame-08 test still for real frame-sequence playback with live character/action pickers. Frame policy: full native 24fps for actions ≤60 frames, half-rate only for the long idle loops — not the 4–6 frame flip-book first floated. Fixed a launch crash along the way: `CAKeyframeAnimation` can't animate `SCNMaterial.diffuse.contents` through an array of `NSImage` (SceneKit's CA bridge only understands animatable scalar/vector/color values and throws), so playback now runs via an `SCNAction` sequence instead. Two more bugs found by actually watching it run: playback was hardcoded to 12fps regardless of how a sequence was sampled, so full-rate (24fps) actions played at half real speed — fixed by deriving fps per selection from the real gap between baked frame indices. And Forest Flicker/Pangolin were clipping into the stage platforms because each character's ground-contact line sits at a different fraction of its own baked frame (a function of that file's `CAM_TARGET` z ÷ ortho scale) while the billboards had one fixed node-y each, tuned against whichever character was loaded last — fixed by reading the real platform-top heights out of `DungeonStageScene.build()` and repositioning per-character on every picker change.**

- **2026-08-24 > docs/design/quadruped-animation-pipeline.md, docs/README.md > reusable quadruped animation pipeline proposal > Defines a deterministic template system for sharing walk, idle, wince, attack, special-attack, and future defeat structures across differently proportioned Rigify quadrupeds. Separates universal motion beats, creature-family style, physical attack archetypes, and character adapters; records validation, generation, versioning, non-animation boundaries, and a three-template proof of concept.**

- **2026-08-10 > release > v0.1.0-alpha.10 (build 10) > The delve, properly playable. Two dead ends found in a Cache Warren run and fixed: gear you win is now gear you keep — the simulation had been resetting the inventory and loadout to the starter item on every needs tick and XP event, destroying each drop seconds after the card awarded it — and a cleared Warren has a way out again, after the end screen's content outgrew the fixed panel and took the Return button off the bottom edge with it. Also carries the playtest polish from the same branch: the three Approaches now behave differently, the gear rail renders as a paper doll, every item has its own glyph, and the model bay breathes.**

- **2026-08-10 > Sources/CompanionCore/PetBrain.swift, PetState.swift, Tests/CompanionCoreChecks/ItemChecks.swift > the simulation stops handing back your loot > Every piece of gear won since items shipped was being silently destroyed within seconds of the drop card claiming it. `PetBrain` rebuilt `PetState` field by field on each write, so every field it did not know to pass fell back to the memberwise initialiser's defaults — and for `ownedItems`/`loadout` that default is the starter rubber duck. Any needs tick or XP event therefore reset the entire inventory and loadout, then persisted the reset; gear survived only as long as nothing happened. All six brain write paths now go through a new `PetState.advanced(...)` wither that carries forward every field it does not move, restoring the file's own rule that there is exactly one full-field copy site. Checks cover both the needs-tick and the XP-event path, and were confirmed to fail against the old behaviour. Note: gear already lost to this cannot be recovered from an affected save.**

- **2026-08-10 > Sources/Worklings/CombatPanel.swift > a cleared Warren has a way out again > Winning the delve stranded the player: the end screen's content ran roughly 140pt past the fixed 600×480 panel once the boss's drop card was on stage, the centred stack clipped the overflow off both ends, and the Return button is the last thing at the bottom — so a full clear had no exit but quitting the app, which then appeared to cost the gear (see above). The screen now compacts when the drop card is up (smaller headline and victor, tier and XP on one row), scrolls if it still overflows, accepts Return/Esc, and carries a quiet Leave affordance from the moment a result exists. The bank/push card gets the same scroll guarantee — it fits today, but Bank and Push are the buttons that strand a run mid-delve if they ever fall off the edge.**

- **2026-08-09 > Sources/Worklings/CombatPanel.swift, PetSession.swift, Tests/CompanionCoreChecks/DelveChecks.swift > a mid-delve drop is yours the moment you win it > A cleared encounter now banks its item into the inventory immediately, instead of waiting for the delve write-back. The drop card's Equip button is ownership-gated, so while the grant was deferred it silently did nothing for every drop except the boss's — clicking Equip mid-delve appeared to do nothing because it genuinely did nothing. The card now also says the item is already in your bag and offers "Keep in bag" as a real inverse of Equip, so declining is a visible choice rather than an unanswered click. The bank/push stakes line no longer claims all gear waits at the bottom; only the Prime tier does.**

- **2026-08-09 > Sources/CompanionCore/CombatEncounter.swift, PetCombat.swift, Sources/Worklings/CombatPanel.swift, Tests/CompanionCoreChecks/CombatChecks.swift > the three Approaches now do three different things > Careful used to brace *every* round below 50% HP and never attack again; against a Monolith that made the fight unwinnable and unwatchable — the pet stopped acting, so the boss looked like it was attacking back to back. Careful now alternates Brace and Strike inside a hysteretic band (enters at 40% HP, leaves at 60%), and brace regen scales with the HP pool (8%, floored at the old flat 2) instead of decaying into a rounding error. Clever was a byte-for-byte copy of Aggressive; it now holds the Signature and spends it unprompted once the foe is under 35% HP. Both the briefing and the mid-fight decision controls print what the selected Approach actually does, generated from the live tuning so the copy can't drift from the rules.**

- **2026-08-09 > Sources/Worklings/CharacterScreenView.swift > the model bay breathes > ModelBay now animates instead of holding one static frame: a TimelineView blinks the sprite for a single tick roughly every three seconds (rather than the desktop companion's half-open/half-shut alternation, which strobes at this size), over a slow breathing bob whose contact shadow tightens as the Workling rises so the motion reads as weight. Reduce Motion holds the open-eyed frame with no bob.**

- **2026-08-09 > Sources/Worklings/CharacterScreenView.swift, CombatPanel.swift > the gear slots actually render what they're told to > Root cause of the gear rail looking like a list of words: on macOS a SwiftUI `Menu` label is backed by NSPopUpButton, which flattens the label to a single image plus a single title and discards the rest of the view. Every slot box, item name, stat price, and tier pill written into those labels was being dropped at render, in both the Character screen rail and the briefing loadout bar. Both are now plain Buttons presenting a `GearSlotPicker` popover, which renders the full row — and lets the picker itself show each candidate's glyph, name, price, and equipped state instead of a line of text per item. CareTabView's menus use Label(title, systemImage:) and are unaffected.**

- **2026-08-09 > Sources/Worklings/CharacterScreenView.swift > the gear rail becomes a paper doll > Each slot row now carries a real square slot box on the right: empty is a recessed dashed well with a ghosted slot glyph, equipped is a tier-tinted plate with the item's glyph, a green ring, and a corner check — occupancy readable without reading, and without relying on colour alone. The row also names the equipped item and its stat price, and the loadout total is labelled GEAR TOTAL; previously the rail showed a bare "+2 Wit" with nothing indicating which item produced it. The box is sized for real item art to drop in later, with the glyph as the placeholder.**

- **2026-08-09 > Sources/Worklings/CharacterScreenView.swift, CombatPanel.swift > every item gets its own glyph > Adds a per-item icon (itemIcon) used by the gear rail, the inventory, the briefing loadout bar, and the drop reveal, replacing a per-slot glyph that made every Tool look like every other Tool and a generic crate on every drop. Inventory tiles gain a tier-tinted icon plate with an equipped checkmark, so the list reads as objects rather than paragraphs. Item artwork proper does not exist yet — this is iconography, not art.**

- **2026-08-09 > Sources/Worklings/CharacterScreenView.swift, CombatPanel.swift > gear slots you can read at a glance > Filled and empty gear slots were separated only by a text colour and a background opacity, so an equipped item was invisible and its stat bonus appeared to come from nowhere. Both the Character screen rail and the briefing loadout bar now give an occupied slot a tier-tinted well, border, item glyph, stat price, and tier name, and an empty one a dashed outline around a ghosted slot glyph.**

- **2026-08-09 > release > v0.1.0-alpha.9 (build 9) > The delve update. A single fight becomes a journey: a narration briefing, a loadout you pack before descending, a four-encounter chain through the Cache Warren, and a bank-or-push prompt between every fight. Gear arrives as a real system — three functional slots (Tool/Ward/Charm), fifteen items across three tiers, and a drop off every encounter, with the boss's Prime gear as the reason to walk past the bank prompt. Clicking your Workling now opens a Character Screen: gear rail, model bay, and Character/Inventory/Skills/Care tabs, replacing the old care popover.**

- **2026-08-09 > Sources/CompanionCore/Items.swift, Delve.swift, PetState.swift, Sources/Worklings/CombatPanel.swift, CharacterScreenView.swift > three item tiers, and every fight pays > Adds Scavenged/Solid/Prime tiers and grows the catalogue to fifteen items (each of the five stats at every tier), with the original five becoming the Solid tier and their raw values preserved so old saves read back unchanged. Every cleared encounter now drops one item of its depth's tier — the mini-boss Prime, the last regular encounter Solid, everything shallower Scavenged — rather than a single payout at the bottom. An exhausted tier drops nothing rather than substituting another. Gear won is kept on a bank and on a retreat alike; what banking forfeits is the depth (the completion bonus and the Prime item).**

- **2026-08-09 > Sources/Worklings/CombatPanel.swift > say what banking costs, before it costs it > The bank/push prompt now states the forfeit outright and names gear only while there is still gear to win, tells you how many encounters remain, and the briefing says up front that only what waits at the bottom carries gear. Previously the XP you kept was visible and the completion bonus and gear you gave up were not, so a player who banked early never learned the deeper rewards existed.**

- **2026-08-09 > Sources/CompanionCore/Items.swift, PetState.swift, Sources/Worklings/CombatPanel.swift, PetSession.swift, AppDelegate.swift > the drop beat > Gives gear its own staged reveal instead of a line of text: the item card lands after the victory fanfare thins out, Return waits for it, and an Equip button puts it on without leaving the screen. GearSwap states what equipping would cost — because items are mono-stat and slot-bound, a swap usually moves two different stats in opposite directions. Adds a debug-only "Forget Acquired Gear" reset so the drop loop can be iterated on without exhausting it.**

- **2026-08-09 > Sources/CompanionCore/CharacterSheet.swift, Sources/Worklings/CharacterScreenView.swift, CharacterWindowController.swift, GearPricing.swift, CareTabView.swift, CompanionPanelController.swift, AppDelegate.swift > the Character Screen hub > Clicking a Workling opens a free-resize floating hub instead of the old care popover: a persistent left rail (model bay plus the three gear slots) beside Character, Inventory, Skills, and Care tabs. The stat table shows base, gear, and total side by side, and the "in the arena" readout is taken off the same Combatant the resolver builds, so the screen cannot drift from the fight. The care popover is retired and its content becomes the Care tab, so care data lives in exactly one place.**

- **2026-08-01 > release > v0.1.0-alpha.8 (build 8) > The combat update. Distinct foe abilities on a new status-effect system — Snag's Snare (Agility debuff), Flicker's Blur/Phase and Unleash opening, and the Monolith mini-boss's telegraphed Slam and phase-threshold Harden; full combat audio (dungeon soundtrack, separate boss theme, per-action SFX, mute toggle + volume slider); combat-feel fixes (steady combatants, action announcement holds through the swing); a per-foe encounter picker; and a platform-portable (CoreGraphics-free) core.**

- **2026-08-01 > assets/audio/dungeon/*, Sources/Worklings/CombatAudio.swift, CombatPanel.swift, AppDelegate.swift, Package.swift > full dungeon combat audio > A looping dungeon BGM with a separate boss theme, and per-action cues (hit, crit, slam, dodge, snare, phase, telegraph, harden, victory, defeat, enter, return) generated deterministically via the audio-lab pipeline; wired through a defensive AVAudioPlayer manager with a paw-menu mute toggle and master volume slider.**

- **2026-08-01 > Sources/CompanionCore/StatusEffect.swift, Combat.swift, CombatResolver.swift, CombatEncounter.swift, Bestiary.swift > distinct foe abilities > Adds a status-effect primitive (timed buffs/debuffs folded into effective stats) and foe behavior archetypes as bestiary data, then gives each foe its own mechanic: Snag grabs to Snare the pet's Agility, Flicker blurs/phases and over-extends into an Unleash opening, and the Monolith telegraphs a heavy Slam (a Brace-or-eat decision) and hardens at HP-phase thresholds.**

- **2026-08-01 > Sources/Worklings/CombatPanel.swift > combat-feel fixes > Fixes combatants sliding sideways when speech bubbles/countdown appear (fixed-width columns), and keeps the action announcement up through the countdown and the swing so it stays readable.**

- **2026-08-01 > Sources/CompanionCore/ScreenPlacement.swift, Sources/Worklings/PlacementGeometry+AppKit.swift > platform-portable core > Removes the last Apple-only dependency (CoreGraphics) from CompanionCore with a small platform-free geometry type, so the game engine can build for non-Apple platforms later; the app bridges to CGGeometry at the boundary.**

- **2026-07-31 > videos/meet-the-worklings/index.html, compositions/*.html, STORYBOARD.md, frame.md, package.json, hyperframes.json, meta.json > main timeline and five modular scene timelines > Adds an 18-second 1080×1920 HyperFrames social composition that opens a living field guide, introduces Wildkin, Elemental, and Relicborn through companion-to-signature pose sequences, and resolves into a smoke-assisted three-character hero lockup. Uses deterministic GSAP motion, portrait-safe typography, velocity-matched scene pushes, and the repository's established Broadside visual system.**

- **2026-07-31 > videos/meet-the-worklings/audio-lab/manifest.yaml, audio-lab/library/provenance/, assets/audio/ > Audio Asset Lab build and verification > Defines and reproducibly generates a bespoke 18-second instrumental music bed, character-specific leaf/ember/clockwork transition cues, a magical smoke poof, and a three-note collection flourish using explicit engines and fixed seeds; exports WAV/OGG delivery files and verifies hashes and provenance before HyperFrames mounts the cues.**

- **2026-07-31 > videos/meet-the-worklings/audio-lab/manifest.yaml, audio-lab/library/candidates/, audio-lab/library/masters/, audio-lab/library/provenance/, audio-lab/exports/, assets/audio/ > short SFX generation and seed selection > Regenerates all five model-driven transition effects as three-second, three-seed candidates, rejects saturated candidates, selects clean early-action takes, and crops them to their original 1.2–1.5-second delivery lengths before fading and normalization.**

- **2026-07-31 > videos/meet-the-worklings/compositions/01-catalogue-open.html > hook portal timing marker > Marks the timed opening portal as a HyperFrames clip so its scheduled visibility validates correctly.**

- **2026-07-31 > videos/meet-the-worklings/renders/meet-the-worklings-18s.mp4 > final high-quality HyperFrames render > Exports the approved vertical social video as an 18-second 1080×1920 H.264 MP4 with 48 kHz stereo AAC audio.**

- **2026-07-31 > videos/meet-the-worklings/assets/characters/, assets/effects/, assets/fonts/, .media/ > project-local first-party media inventory > Packages the three five-row character sprite sheets, eight-frame smoke effect, and established Teko/Inter/IBM Plex Mono fonts into the isolated video project, with a local media ledger so the composition remains portable and its source assets stay traceable.**

- **2026-07-31 > Sources/CompanionCore/Bestiary.swift, Sources/Worklings/CombatPanel.swift, Tests/CompanionCoreChecks/CombatChecks.swift > rename foe Mote → Dungeon Scamp > Changes the first foe's display name to "Dungeon Scamp". The display name is also the sprite join key (name → `mote-*` assets), so the loader's mapping is remapped to the new name while the art files stay `mote-*`, and a check now pins the name so a silent rename can't drop the Scamp's art.**

- **2026-07-31 > Sources/Worklings/CombatPanel.swift > hold combat text longer > Increases the per-beat hold durations across `beats(for:)` (wind-ups, hits, misses, brace, signature, defeat) so each line of narration stays on screen long enough to read comfortably after the 3-2-1 countdown pacing pass.**

- **2026-07-31 > Sources/Worklings/CombatPanel.swift > scene-setting narration as a centered banner > Splits scene-setting narration (the encounter's opening line) out of the combatants' speech bubbles into a distinct centered, italic banner across the top of the stage (new `narrativeLine` channel on the view-model and a `NarrativeBanner` view), so the dungeon's own voice reads differently from per-hit action lines.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift > 3-2-1 countdown before each action > Restructures the beat flow into a clear rhythm: an intro line naming the coming action, then a big centre 3 → 2 → 1 countdown (each number pops in and out), then the swing and the hit land together on one beat. Gives every attack anticipation and paces the fight so it's easy to follow. During the countdown the bubbles hide and both creatures idle.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift > poof the loser at defeat, and a clean winner-only victory screen > The smoke now fires the instant a fighter is defeated, over it in the arena (an `ArenaCombatant` poof driven by a new `defeatedSide` signal), instead of late on the end screen. The end screen is now opaque (its own cave background) and shows only the victor — no more the dungeon and both characters showing through behind a dim overlay.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift > stop the atmosphere overlay washing out the stage > The overlay is a near-white haze; drops its opacity from 0.85 to 0.14 so it drifts as faint atmosphere instead of a white veil, and strengthens the background darkening so the fighters and text read clearly against the cave.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift, WildkinPetView.swift > victory / defeat end screen > When a fight ends, a full-arena overlay takes over: a big Victory!/Defeated title pops in, the winner takes centre stage in its victory pose with a spring pop-in, a gentle bob, and a ring of twinkling sparkles, while the loser puffs away in the reused 8-frame smoke effect; then the exit tier, XP, and Return button fade in. Makes `SmokeEffectSprite` reusable with a size; moves the old bottom summary into this screen.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift > bigger, higher combat text > Enlarges the event speech bubbles (title-size bold, wider wrap, more padding, a drop shadow) and raises them with a taller reserved slot so each beat's text occupies more of the stage and reads as a more interactive fight.**

- **2026-07-30 > assets/dungeons/cache-warren-cave-backdrop.png, cache-warren-atmosphere-overlay.png, Package.swift, Sources/Worklings/CombatPanel.swift > cave backdrop and drifting parallax atmosphere > Replaces the flat gradient stage with the painted Cache Warren cave backdrop plus a slowly-drifting, seamlessly-looping translucent atmosphere overlay (two chasing copies) for parallax life, over a faint darkening for text legibility. New `ArenaBackground` / `AtmosphereDrift` views and a `DungeonArtAsset` loader (shared `bundledCGImage` helper). Drift freezes under Reduce Motion; falls back to the gradient if art is missing.**

- **2026-07-30 > assets/dungeons/cache-warren-atmosphere-overlay.png > seamless Cache Warren atmosphere layer > Adds a deterministic 1600×900 native-alpha overlay of horizontally periodic pixel-cluster fog, warm drifting dust and embers, and sparse cyan motes; the left/right boundary is byte-identical for seamless scrolling and all atmosphere remains low-opacity so combatants and speech bubbles stay readable.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift > combat impact juice (no new art) > Adds code-driven hit feedback on top of the single-frame sprites: whoever takes damage does a quick squash-and-shake punch, a white flash, and a rising damage number (crits bigger and yellow). The view-model emits a per-side hit token/amount/crit on each damaging beat; a new `ArenaCombatant` view owns the transient effects and an `ImpactShake` keyframe modifier drives the punch. Makes the current static poses land much harder while real multi-frame animation waits on the Blender pass.**

- **2026-07-30 > assets/dungeons/cache-warren-cave-backdrop.png > Cache Warren arena cave backdrop > Adds a 1200×960 painterly pixel-art cavern background for the 5:4 combat arena, with a broad readable floor, a quiet central combat space, perimeter-weighted buried brass machinery, muted warm lighting, and restrained cyan rune cracks.**

- **2026-07-30 > assets/foes/mote-idle.png, mote-attack.png, mote-hurt.png, Package.swift, Sources/Worklings/CombatPanel.swift > wire the Mote's combat sprites > Registers the Mote's idle/attack/hurt PNGs as resources and renders them in the arena via a new `FoeSprite` view and `FoeSpriteAsset` loader (loaded once, like the family sheets). The foe now gets the same beat-driven treatment as the pet — idle bob at rest, its attack pose (lunging left toward the pet) on its wind-up, and its hurt pose when the Workling lands a hit. Foes without art fall back to the placeholder.**

- **2026-07-30 > assets/foes/mote-idle.png, assets/foes/mote-attack.png, assets/foes/mote-hurt.png > Mote Dungeon Scamp idle, attack, and hurt frames > Adds the selected asymmetric dust-and-lint dungeon adversary as three transparent 256×256 sprites: a hovering idle, leftward headbutt with a compact impact puff, and squished recoil with loose lint, all normalized to a consistent mini-enemy scale for the foe-sprite loader.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift > slow, readable combat beats with descriptor text > Replaces the flat one-event-per-tick reveal with a beat script: each strike plays a wind-up ("<name> attacks the <foe>.") then a result ("<name> hits the <foe> for N damage." / "<foe> dodges!"), each held ~1.2–1.8s so it's readable, with the foe's wind-up serving as its 'gearing up' beat. Displayed HP now drops on the result beat (not instantly), the pet holds its strike/hurt/brace/signature pose through the beat, and the speech bubbles carry full sentences (wrapped, wider slot). Pacing no longer collapses under Reduce Motion, so text always stays readable.**

- **2026-07-30 > Sources/Worklings/AppDelegate.swift, CombatPanel.swift > pet leaves the desktop for the arena and returns > Entering a delve now hides the desktop companion (its smoke conceal) so it 'appears inside' the dungeon screen, and brings it back when the fight ends. `CombatPanelController.present` takes an `onDismiss` that always runs on close, and the panel drops its title-bar close button so the in-panel Close/Return is the single exit — the companion's return can never be skipped.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift > combat as a face-off arena with speech bubbles > Rebuilds the combat panel into a larger (600×480) dungeon stage: the pet and foe stand off facing each other over a shadowed ground, the pet flipped to face the foe, blinking while idle and lunging into its action pose on a strike. Narration moves out of the inline log into comic speech bubbles above the acting creature (damage, crit, miss, brace, unleash, victory). Bigger sprites (150px), HP bars and counts under each combatant. Foe is a gently-bobbing placeholder until its sprites land.**

- **2026-07-30 > Sources/CompanionCore/Bestiary.swift, Tests/CompanionCoreChecks/CombatChecks.swift, docs/design/dungeons.md > tune the Mote for a few rounds > Raises the Mote from 8 HP to 30 (with a small Guard/Agility/Power bump) so a Level-3 Workling can't one-shot it — the warm-up now runs two to three rounds of back-and-forth instead of ending on the first strike. Check and bestiary table updated.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift, WildkinPetView.swift > render the real Workling sprite in combat > Makes `WorklingSprite`/`WorklingSpriteFrame` reusable (adds a `size` parameter) and renders the pet's family sheet in the combat panel instead of the SF Symbol placeholder, with the pose driven by each narrated beat — Strike on its attack, Hurt when hit, Brace/Signature on those actions, Victory/Downed at the end, and Low-HP at rest once hurt enough. The foe stays a placeholder until foe sprites land.**

- **2026-07-30 > assets/worklings-wildkin-spritesheet.png, assets/worklings-elemental-spritesheet.png, assets/worklings-relicborn-spritesheet.png, Sources/Worklings/WildkinPetView.swift, docs/design/sprite-prompts.md, docs/design/dungeons.md > WorklingSpriteFrame combat cases and five-row sprite-sheet contract > Extends all three family sheets from 4×3 to 4×5 while preserving the original twelve cells, adds generated Strike/Hurt/Low-HP/Victory and Downed/Brace/Signature frames in fixed rows 3–4 with the last cell unused, maps those seven cases explicitly in the renderer, and corrects the design docs' invalid 4×4/optional-pose description.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift, AppDelegate.swift > combat panel window and the delve entry point (vertical slice, step 13) > Adds `CombatPanelController` (a floating titled panel hosting the combat view for one fight, then torn down) and the paw-menu entry 'Enter the Cache Warren…'. The item enables only when `delveBlock` clears, otherwise its tooltip explains why (reach level 3 / the Workling needs care). Entering seeds a fight from the moment of entry and opens the panel against the Mote. This closes the slice: menu → panel → fight → XP + condition write-back → persisted.**

- **2026-07-30 > Sources/Worklings/CombatPanel.swift, Sources/CompanionCore/CombatEncounter.swift > combat panel UI (vertical slice, step 12) > Adds `CombatViewModel` (drives the encounter, reveals the narration log one beat at a time — instant under Reduce Motion — pauses for decisions, writes the result back on completion) and `CombatPanelView` (two combatant rows with HP bars, a scrolling narration log, a segmented Approach picker + Unleash toggle at decision points, and a victory/defeat summary with the exit tier and XP). Combatant art is a labelled SF Symbol placeholder to swap for the real combat sprites. `Approach` gains Hashable for the picker.**

- **2026-07-30 > Sources/Worklings/PetSession.swift > PetSession dungeon hooks (vertical slice, step 11) > Adds the app's bridge to the combat engine: a shared `combatRates`, `delveBlock`/`canEnterDelve` for the entry control, `makePetCombatant()` to start a fight from the live sheet+condition, and `applyCombatResolution(_:)` to write a finished encounter's XP and exit-tier condition change back into the pet and persist.**

- **2026-07-30 > Sources/CompanionCore/CombatRewards.swift, PetCombat.swift, Tests/CompanionCoreChecks/CombatChecks.swift > delve entry eligibility (vertical slice, step 10) > Adds the level gate and critical-need refusal: new `delveGateLevel` (3) and `refusalNeedThreshold` (10) knobs, plus `PetCombatRates.delveBlock(for:)` returning a `DelveBlock` reason (belowGateLevel / needsCare) and `canEnterDelve(_:)`, so the UI can explain a disabled delve rather than just greying it out. Checks cover a below-gate pet, a healthy leveled pet, and a critical need blocking entry.**

- **2026-07-30 > Sources/CompanionCore/Combat.swift, Tests/CompanionCoreChecks/CombatChecks.swift > build the pet combatant from PetState (vertical slice, step 9) > Adds `Combatant.pet(from:rates:)`, reading name, the earned stat sheet, and current condition straight off the live `PetState`, so the app can start a fight without unpacking state. Checked against building from the parts.**

- **2026-07-30 > Sources/CompanionCore/CombatRewards.swift, PetState.swift, Tests/CompanionCoreChecks/CombatChecks.swift > exit tier and reward write-back (vertical slice, step 8) > Closes the domain loop. `ExitTier.forOutcome(victory:hpFraction:)` (flawless/solid/barely, downed on a loss), a `ConditionDelta`, and `PetCombatRates.exitConditionDelta(for:)` holding the exit-tier table. `PetState.applyingOutcome(of:foe:rates:)` grants the foe's reward XP on a win and moves all four conditions by the tier (Fullness gain applied as a hunger cut), returning an `EncounterResolution`. Adds a combat-agnostic `PetState.applying(needs:addingXP:)` wither (extending the private `replacing`). Checks cover tier derivation, XP+condition movement on a win, downed/no-XP on a loss, and the reversible-neglect clamp at 0 and 100.**

- **2026-07-30 > Sources/CompanionCore/CombatEncounter.swift, PetCombat.swift, SeededGenerator.swift, Tests/CompanionCoreChecks/CombatChecks.swift > the encounter engine (vertical slice, step 7) > Adds `CombatEncounter`: the round loop that ties everything together. Types for `Approach` (aggressive/careful/clever), `CombatAction`, `DecisionReason`, `CombatStatus`, and a structured `CombatEvent` log the UI can narrate/animate. `step()` either pauses for a decision or resolves a full round (both act in Agility initiative order, the pet winning ties); Brace mitigates the incoming blow that round and regens; `decide(approach:unleash:)` resolves a decision and can Unleash the once-per-encounter Signature; `runToCompletion()` is the headless driver. New knobs: decisionCadenceRounds, lowHPEventThreshold, carefulBraceThreshold; SeededGenerator gains Equatable/Sendable so the whole encounter compares by value. Checks: seed replay, aggressive beats a Mote, an outmatched pet is downed, the faster combatant strikes first, and a decision point Unleash fires and consumes the Signature exactly once.**

- **2026-07-30 > Sources/CompanionCore/Bestiary.swift, Tests/CompanionCoreChecks/CombatChecks.swift > Cache Warren bestiary data (vertical slice, step 6) > Adds a `Foe` type (name, maxHP, stat block, reward XP, `makeCombatant()`) and the Cache Warren foes inline as data — Mote/Snag/Flicker/Monolith with the held stat blocks and kill rewards, plus the ordered encounter list and boss. Behaviour and named abilities stay for a later slice (v1 foes just attack). Checks guard the stat blocks against the spec and confirm a fresh foe starts at full HP.**

- **2026-07-30 > Sources/CompanionCore/CombatResolver.swift, PetCombat.swift, Tests/CompanionCoreChecks/CombatChecks.swift > Brace and Signature actions (vertical slice, step 5) > Adds the rest of the pet's action set. New rates: braceMitigation, braceRegen, signatureMultiplier. `resolveStrike` gains a `damageMultiplier` the loop passes as braceMitigation when the defender is Bracing (a braced blow lands for less); `resolveSignature` is a guaranteed hit (no dodge/crit) at the signature multiplier, still drawing its variance from the seeded stream. Checks confirm bracing never raises damage taken (same-seed comparison) and the Signature always lands and hits harder than a base Strike.**

- **2026-07-30 > Sources/CompanionCore/CombatResolver.swift, PetCombat.swift, Tests/CompanionCoreChecks/CombatChecks.swift > single Strike resolution (vertical slice, step 4) > Adds `CombatResolver.resolveStrike(...)` and `StrikeOutcome` (didHit/didCrit/damage). Draws in a fixed, documented order — hit, then crit, then the damage swing — against the seeded stream, applies the new `strikeVariance` (±15%) and crit multiplier, floors damage at 1, and mutates the defender's HP. Checks cover seed determinism (identical sequence on replay), miss→0-damage / hit→≥1, damage staying within the variance/crit bounds, and the observed hit rate converging on the 0.54 formula.**

- **2026-07-30 > Sources/CompanionCore/Combat.swift, PetCombat.swift, Tests/CompanionCoreChecks/CombatChecks.swift > combatant model and effective stats (vertical slice, step 3) > Adds `CombatStats` (power/defense/agility/wit) and `Combatant` (name, stats, transient maxHP/currentHP with clamped takeDamage/heal, isDefeated, hpFraction). A `Combatant.pet(...)` builder scales base `PetStats` by a new `combatEffectiveness(needs:)` (reusing the needs average behind the progression multiplier, with a 0.5 combat floor) and derives maxHP from scaled Vitality; `Combatant.foe(...)` uses the raw stat block since condition never scales a foe. Checks cover full-condition parity (Aegis maxHP 41), neglect scaling to the floor, foes ignoring condition, and HP clamps.**

- **2026-07-30 > Sources/CompanionCore/SeededGenerator.swift, Tests/CompanionCoreChecks/CombatChecks.swift > deterministic seeded PRNG for combat (vertical slice, step 2) > Adds `SeededGenerator`, a SplitMix64 `RandomNumberGenerator` so combat rolls replay identically from a seed (save state + per-delve nonce), keeping the deterministic-simulation contract. Includes a `chance(_:)` probability helper (clamped; 0 never, 1 always) as the single yes/no roll entry point. Checks cover same-seed replay, seed divergence, the extremes, and a stable near-half distribution for a fair coin.**

- **2026-07-30 > Sources/CompanionCore/PetCombat.swift, Tests/CompanionCoreChecks/CombatChecks.swift, CheckRunner.swift > combat rates and core formulas (vertical slice, step 1) > First code slice of the dungeon combat engine: a `PetCombatRates` struct holding the held combat knobs (baseHP, vitalityToHP, powerScale, guardScale, hit-chance base/scale/clamps, crit chance/multiplier) with clamped defaults, and the four pure resolution formulas — `maxHP(vitality:)`, `strikeDamage(power:targetGuard:)` (floored at 1), `hitChance(attackerAgility:defenderAgility:)` (clamped), and `critChance(agility:)`. Behavioral checks trace the worked Flicker example from the design doc (Level-3 Aegis maxHP 41, strike 8.5, hit 0.54; Maverick hit 0.66; clamps and the damage floor). Pure and deterministic; no state, RNG, or app wiring yet.**

- **2026-07-30 > docs/design/dungeons.md > enemy abilities and a first-encounter build plan > Expands the Cache Warren foes with behavior scripts and named abilities (Snag's Snare, Flicker's Blur/Phase, Monolith's telegraphed Slam and HP-threshold phases), noting the shared status-effect/telegraph primitive they and the class abilities both need. Adds a 'What a first playable encounter needs' section: a recommended vertical slice (one foe, three actions, one decision point, one reward), the CompanionCore domain pieces (combat state, seeded PRNG, resolver, foe data, orchestration, checks), the app pieces (combat surface, entry point, sprite wiring, pacing, narration, accessibility), additive persistence, the content/sprite minimum, and the open build decisions (combat surface, foe-sprite pipeline, slice-vs-delve, save migration). Knobs noted, values held.**

- **2026-07-30 > docs/design/abilities.md, docs/design/items.md, docs/README.md, docs/design/gameplay-loop.md > ability and item design layers > Adds two design docs above the dungeon combat model. Abilities: the ability-point currency (earned from delves, separate from stat growth), how an ability acts at a combat decision point, the shared status-effect primitive combat needs, and the first class-specific ability each (Wellspring Second Wind, Juggernaut Overbear, Aegis Bulwark, Maverick Flurry, Tinkerer Expose) as the Signature seeds promoted into costed actions — read against the Flicker as five different solutions. Items: the read-time effective-stats model (base + gear × condition, never persisted into base), a minimal Tool/Ward/Charm slot set, a dual-coded base item set (Rubber Duck, Cracked Whetstone, Dented Buckler, Quickstep Charm, Warm Backup-Coal), sourcing via delve drops, and additive persistence. All magnitudes held as knobs. Wired into the umbrella and index.**

- **2026-07-30 > docs/design/dungeons.md > full dungeon combat spec with concrete numbers > Expands the dungeon doc from a design sketch into a complete, numbered spec: the round loop, three pet actions (Strike/Brace/Signature) with formulas grounded in the real stat scales (maxHP, strike, hit chance, crit), decision points (every-3-turns cadence + event triggers), the condition↔combat coupling with a 0.5 combat effectiveness floor, a critical-need refusal, inter-encounter regen, and exit-tier condition deltas. Lays out the Cache Warren delve (Mote/Snag/Flicker/Monolith stat blocks and mechanic curve), a fully numbered Flicker walkthrough with a Level-3 Aegis, per-class numbers off the same fight, the reward/XP-channel economy, and a consolidated Tuning knobs table (proposed PetCombatRates) for the dial-up/dial-down discussion. Numbers are first-pass proposals.**

- **2026-07-30 > docs/design/sprite-prompts.md, docs/design/dungeons.md, docs/README.md > sprite generation prompts for the dungeon > Adds a copy-paste prompt pack for the new art the dungeon needs: a shared global pixel-art style block, per-family character anchors (Wildkin moss-fox, Elemental ember-newt, Relicborn keyback pangolin) pulled from the existing sprites, the five core combat poses (Strike, Hurt, Low-HP, Victory, Downed) plus optional Brace/Signature to generate per family, and the Cache Warren foes (Mote, Flicker, Snag, Monolith). Records the 256px 4-column sheet/code contract so generated poses drop straight in. Links it from the dungeon doc and the docs index.**

- **2026-07-30 > docs/design/dungeons.md, docs/design/gameplay-loop.md, docs/README.md > first-draft dungeon encounter design > Adds the design direction for the first content system: solo delves built on the existing progression sheet and condition layer, resolving against stats + class + condition only (abilities/gear slot in later). Specifies an active-auto-battler combat model (turn-based, auto-resolving, narrated per turn) whose core lever is n-turn encounters steered at decision points — a standing Approach re-chosen on a turn cadence and on triggered events. Maps the five stats to combat roles, and defines the closed condition↔combat loop: a separate transient combat-HP pool scaled by the care effectiveness multiplier on the way in, with delve-exit HP tiers moving all four conditions on the way out (best case gains across the board, worst case losses across the board), all inside the reversible-neglect envelope. Includes a worked example encounter (the Flicker), a per-class walkthrough that seeds each class's first ability, the delve frame, and the new sprite states combat needs. Frames the machine-underworld setting as the first dungeon's, not the canonical world — dungeons can span many work/productivity universes. Links it into the gameplay-loop umbrella and the docs index.**

- **2026-07-30 > docs/ (all), README.md, AGENTS.md, CONTRIBUTING.md > reorganize documentation into a foldered structure > Groups the docs into `design/` (pet-brain, progression, characters, interaction), `engineering/` (architecture, cross-platform-architecture, adapters), and `process/` (git-workflow, distribution, audit-followups), keeping `product-brief.md` and `changelog.md` at the docs root; renames all files to kebab-case; adds a `docs/README.md` index and a `design/gameplay-loop.md` umbrella that frames how care, progression, and planned content fit one loop; rewrites every cross-doc, README, CONTRIBUTING, and AGENTS link to the new paths. Historical changelog entries keep their original paths, since they record the tree as it was at the time. Makes room for the coming abilities, dungeons, and class/family design docs.**

- **2026-07-30 > AGENTS.md > repository browser-automation guidance > Requires confirmation before the agent uses browser automation, prefers handing browser steps to the user when that is quicker or more token-efficient, and treats an explicit browser request in the current task as confirmation.**

- **2026-07-30 > docs/cross_platform_architecture.md, docs/architecture.md > cross-platform architecture roadmap and technology decision gates > Records why the current Swift/AppKit implementation should remain macOS-first, defines a future platform-neutral `WorklingsDomain` with `CoreGraphics`, persistence, hooks, and other operating-system concerns outside it, preserves versioned save/activity contracts through golden behavioral fixtures, compares Godot, Flutter, Tauri/Rust, and native hosts, and establishes staged product, safety, prototype, and platform-selection gates before any expansion or rewrite.**

- **2026-07-29 > Sources/CompanionCore/HookConfigMerger.swift, Sources/Worklings/AppDelegate.swift, docs/adapters.md, docs/audit_followups.md > guard the written hook so a deleted app leaves no errors > The command Worklings writes into each tool now guards the adapter with an `[ -x <adapter> ]` existence test, so if the app is dragged to the Trash the leftover hook degrades to a silent no-op (exit 0, Codex still prints a valid `{}`) instead of erroring on every turn (Codex `127`, Claude a launch failure). Claude uses `/bin/sh -c` with the path as a quoted positional argument — path-safety intact — and Codex an inline-guarded shell string. Ownership/liveness matching was reworked to find the adapter path wherever it appears (command, a single-quoted shell word, or an `args` element), keyed on the distinctive `worklings-…-activity-hook` file name, so both old and guarded forms are recognized and cleaned up. Two checks run the generated commands with a missing adapter and assert the clean no-op. This is the convention dotfile tools use (e.g. `nvm`'s `[ -s … ] &&` line); the consent dialog now also says to Disconnect All Tools before deleting Worklings.**

- **2026-07-29 > Sources/Worklings/AppDelegate.swift, docs/architecture.md, docs/audit_followups.md > remember tool-connection consent per tool > The one-time consent disclosure is now recorded per tool (`toolConnectionConsentAcknowledged.<tool>`) rather than globally. Because each tool edits a different file, approving Claude Code no longer suppresses Codex's own "exact file being changed" disclosure — connecting Codex later shows it once for Codex.**

- **2026-07-29 > Sources/CompanionCore/ToolConnector.swift, Sources/Worklings/AppDelegate.swift, docs/audit_followups.md > follow-up review: close the backup-window race and stop mistaking an unreadable config for "not connected" > The concurrent-edit guard compared the file before making the backup, leaving the backup step itself inside the check→write window — a mid-backup edit by another program could still be overwritten. Now each attempt backs up, then does the confirming re-read, then writes, with nothing but the atomic rename between the compare and the write. Separately, a config that exists but is unreadable or not valid JSON now reports a distinct `.unknown` connection state instead of collapsing to "not connected": the menu shows "… — can't read config", connecting explains rather than blindly writing, and "Disconnect All Tools" surfaces it as a cleanup failure instead of falsely reporting "no hooks were found".**

- **2026-07-29 > Sources/Worklings/AppDelegate.swift, ActivityInboxMonitor.swift, GitCommitWatcher.swift, docs/adapters.md, docs/architecture.md, docs/audit_followups.md > informed-consent dialog; drop the Accept Work Tool Events toggle > The first time you connect any tool, a one-time consent dialog states which file changes (backed up, existing settings preserved), what Worklings receives (an activity kind, source, and time — never a prompt, diff, path, or content), that it all stays local, and how to disconnect; declining writes nothing. The "Accept Work Tool Events" toggle is removed — it was testing scaffolding, and connecting a tool is now itself the opt-in, exactly how connecting a repo already works, so the inbox monitor simply always drains and delivers. This closes the "connecting silently enables event acceptance" gap at the root.**

- **2026-07-29 > Sources/CompanionCore/HookConfigMerger.swift, ToolConnector.swift, Sources/Worklings/AppDelegate.swift, docs/adapters.md, docs/audit_followups.md > distinguish live from stale hooks, and add Disconnect All Tools > A hook command points inside `Worklings.app`, so moving or deleting the app used to leave a dead command that Worklings still called "connected" from its file name alone. Ownership (the adapter file name, which survives an app move) and liveness (does that path still resolve) are now separate: `connectionState()` returns not-connected / live / stale, the paw menu shows a moved app as "Reconnect … — adapter moved" so one click repoints it, and a new "Disconnect All Tools" removes every Worklings hook from both tools in one backed-up step — the clean pre-uninstall path. `adapters.md` documents disconnecting and the harmless-stale-hook behavior when the app is trashed without disconnecting first.**

- **2026-07-29 > Sources/CompanionCore/ToolConnector.swift, docs/audit_followups.md > close the connector's concurrent-edit race > The connector now re-reads the config immediately before writing and compares it to what it merged from; if another program (Claude, Codex) or the user changed the file in that window, it recomputes the merge on the new contents and retries, so a simultaneous edit is preserved rather than overwritten. After a few racing passes it stops with `configChangedDuringWrite` rather than write stale data. (A microsecond window remains between the confirming read and the atomic rename — closing it fully would need file locking the tools don't share.)**

- **2026-07-29 > scripts/adapters/worklings-codex-activity-hook, docs/adapters.md, docs/audit_followups.md > Codex Stop-hook output + deferred-findings backlog > The Codex adapter now prints `{}` on stdout: Codex's `Stop` hook expects JSON on a `0` exit (empty output is invalid there), and an empty JSON object is a valid, content-free success payload for every event, so it never trips a hook-output warning. Captures the remaining post-alpha.5 audit follow-ups in `docs/audit_followups.md` (uninstall/stale-hook liveness, concurrent-edit re-read, informed-consent dialog, and the hook-naming item — found blocked, as neither Codex nor Claude Code documents a hook name field).**

- **2026-07-29 > Sources/CompanionCore/HookConfigMerger.swift, ToolConnector.swift, scripts/adapters/worklings-claude-code-activity-hook, scripts/adapters/worklings-codex-activity-hook, scripts/build_app_bundle, scripts/verify_release, docs/adapters.md > third-round audit: safe ownership identity and fail-closed connector > The adapters are renamed to Worklings-namespaced identifiers (`worklings-claude-code-activity-hook`, `worklings-codex-activity-hook`) so that matching a hook by its file name — which is what lets a moved or reinstalled app bundle still clean up the hooks it wrote — can no longer accidentally claim an unrelated executable that reuses a generic stem like `codex-hook`. The connector now fails closed when an existing config is present but unreadable (a read error is no longer treated as an empty file, so a live config is never replaced after a failed read), and ownership matching parses a single-quoted command back to its literal path so a path containing an apostrophe round-trips through connect/detect/disconnect. Release verification now checks the bundled adapters and emitter are present and executable, and the manual docs are corrected to match exactly what the app writes.**

- **2026-07-26 > Sources/CompanionCore/HookConfigMerger.swift, ToolConnector.swift, Sources/Worklings/GitCommitWatcher.swift, GitRepository.swift, ActivityInboxMonitor.swift, AppDelegate.swift, scripts/build_app_bundle > second-round audit hardening of the connector and git watch > The in-app connector will not erase or over-claim a user's config: unfamiliar `hooks` structure is refused rather than overwritten, ownership is matched per hook by the adapter's exact file name (a sibling hook sharing an entry survives disconnect, and a similarly-named user script is never claimed), and commands are written shell-safely per tool (Claude Code's exec form needs no quoting; Codex's shell form single-quotes the path). It verifies the adapter exists before writing, restricts Claude's Notification hook to await-relevant types, and tells the user Codex hooks need `/hooks` approval to activate. The app bundle now ships the adapters and emitter so the connector works on a DMG install. Git watching no longer touches the main thread even on connect, and the inbox checks a file's type and size before reading it.**

- **2026-07-26 > Sources/Worklings/GitRepository.swift, GitCommitWatcher.swift, ActivityInboxMonitor.swift, AppDelegate.swift, Sources/CompanionCore/ActivitySources.swift, ToolConnector.swift > git-watch and inbox hardening from a branch audit > Never hang, never brick, never wreck a tool session. Every git call runs under a 5s timeout and off the main actor, so a stale network mount or wedged repo cannot freeze launch; a large forward jump (e.g. a pull) is capped per check and counts only commits made while watching (recency-filtered, so fast-forwarding over old history earns nothing); connected repos canonicalize to their top-level so a subdirectory, symlink, or `..` path cannot double-count, and the first commit in a repo that was empty when connected now counts. The activity inbox always drains (files never accumulate) but only delivers while enabled, so toggling off discards events instead of queueing them to replay. The tool connector refuses to write when the adapter script is missing, so a tool is never pointed at a command that fails to launch.**

- **2026-07-26 > scripts/adapters/claude-code-hook, scripts/adapters/codex-hook, Sources/CompanionCore/HookConfigMerger.swift, ToolConnector.swift, Sources/Worklings/AdapterLocator.swift, AppDelegate.swift, docs/adapters.md > agent-tool adapters and a one-tap in-app connector > Claude Code and Codex feed the pet real activity through their lifecycle hooks (the event JSON is drained and discarded on stdin — no prompt, diff, or path is ever read), mapping to workStarted/taskCompleted/workEnded, with Claude Code adding awaitingInput. Instead of hand-editing configs, "Connect Claude Code"/"Connect Codex" in the paw menu write the wiring safely: the existing config is parsed-or-refused, backed up, merged preserving every existing key and hook, and written atomically; disconnect removes only our entries.**

- **2026-07-26 > Sources/CompanionCore/ActivitySources.swift, Sources/Worklings/PetSession.swift > once-per-window reaction throttle > The Workling emotes at most once per window for activity-driven events, so a burst — a batch of commits, several sources at once — celebrates once instead of stuttering robotically. XP and needs still accrue per event; only the expressive reaction is rate-limited, and user-initiated care actions stay immediate.**

- **2026-07-25 > Sources/CompanionCore/ActivitySources.swift, PetProgression.swift, PetState.swift, PetBrain.swift, Sources/Worklings/GitCommitWatcher.swift, GitRepository.swift, ConnectedRepoRegistry.swift, docs/progression.md > in-app local-git activity source with diminishing returns > Commits in repositories connected from the paw menu ("Connected Repos") earn milestone XP, watched in-app via FSEvents on each repo's .git — detection reads only commit SHAs and ancestry, never a message, diff, or path. Successive commits the same day taper geometrically (milestoneDecayFactor) via a new per-source daily count added additively to the save, so a batch of commits tapers toward the cap instead of piling linearly.**

- **2026-07-22 > Sources/CompanionCore/PetState.swift, PetBrain.swift, PetProgression.swift, PetPresentation.swift, ActivityInbox.swift, Sources/Worklings/AppDelegate.swift, PetCareCardView.swift, PetSession.swift, ActivityInboxMonitor.swift, and WorklingsDirectories.swift > structural dedup from the branch code review > Zero-behavior-change refactors that remove the drift-prone copies the review flagged: PetState's three identity withers now share one full-field `replacing` helper so a new stored field is a compile error, not silently dropped state; PetBrain's dailyWake/taskCompleted/milestone cases collapse into one `celebrating` helper; `PetProgressionCurve.progress(forTotalXP:)` becomes the single home of XP-bar arithmetic (also ending the care card recomputing the level four times per render) and `PetPresentation.levelClassLabel` unifies the level/class readout the card and menu header had each spelled differently; AppDelegate's four submenu builders and four checkmark-sync loops become one generic builder and one sync helper; the new `WorklingsDirectories` resolver stops the save store and the inbox independently deriving the Application Support path; and the inbox's accepted-kinds set is now derived from an exhaustive switch, so adding an event kind refuses to compile until it is classified as adapter-emittable or app-owned.**

- **2026-07-22 > Sources/CompanionCore/ActivityInbox.swift and Sources/Worklings/ActivityInboxMonitor.swift > inbox drain moves off the main thread and stops rebuilding parsers > Efficiency pass from the branch code review: the directory watch still fires on the main queue but all listing, reading, decoding, and deleting now runs off the main actor in a coalesced async drain (bursts fold into one follow-up pass), with only event delivery returning to the session; and the inbox decoder plus both ISO8601 timestamp formatters — expensive to construct — become shared statics instead of being rebuilt per file.**

- **2026-07-22 > Sources/CompanionCore/ActivityEvent.swift, PetBrain.swift, Tests/CompanionCoreChecks/ProgressionChecks.swift, and docs/pet_brain.md > idle time no longer counts as focus, and XP scales with pre-action condition > Two gameplay fixes from the branch code review. Focus sessions stop paying for absence: `userReturned` shifts an open work block's `workingSince` forward by the time spent away, and a block ended while still away stops counting at the departure moment, so a block worked 10 minutes, idled 30, worked 5 grants 15 minutes of XP instead of 45. The condition multiplier now reads the needs from *before* the rewarded action or event improved them, so feeding a neglected pet no longer inflates that same grant's multiplier — neglect slows XP exactly as the progression design intends.**

- **2026-07-22 > Sources/CompanionCore/PetBrain.swift, ActivityInbox.swift, Sources/Worklings/ActivityInboxMonitor.swift, PetCareCardView.swift, Tests/CompanionCoreChecks/ProgressionChecks.swift, InboxChecks.swift, and docs/pet_brain.md > event-time correctness fixes from the branch code review > Events are now honored at their own timestamps instead of delivery time: focus-session duration is measured between workStarted/workEnded event timestamps (a session drained late from the inbox is no longer credited for the delay, and the scripted rehearsal now exercises its documented 11 minutes), daily XP caps are charged against the day the event happened rather than the day it was drained, and a drained inbox batch is delivered oldest-event-first regardless of filenames via `ActivityInbox.ordered`. `PetBrain.observe` also advances the simulation with the live activity context, so working/away decay is identical whether the timer or an event advances the clock. The care card's tab picker gains a VoiceOver label, and the pet-brain doc drops a stale save-schema paragraph still promising stored level and banked stat points.**

- **2026-07-22 > Sources/CompanionCore/ActivityInbox.swift, Sources/Worklings/ActivityInboxMonitor.swift, AppDelegate.swift, Tests/CompanionCoreChecks/InboxChecks.swift, CheckRunner.swift, scripts/emit-activity-event, and docs/architecture.md > the activity inbox external adapters write into > Adds the provider-neutral boundary between external tools and the pet: a local spool directory (`Application Support/Worklings/inbox`) any adapter can drop one content-free JSON event file into, watched by the app and validated in pure, checked `CompanionCore` code — app-owned kinds and reserved source ids (`system`, `manual`, `simulated`) cannot be injected or impersonated, malformed/oversize payloads are discarded, and events older than the context expiry never replay onto the pet at launch. Off by default behind a new "Accept Work Tool Events" menu toggle, independent of every other control; `scripts/emit-activity-event` demonstrates the atomic write-then-rename pattern and serves manual testing. Future Codex/Claude Code/git adapters become small scripts emitting into this inbox under their own source ids.**

- **2026-07-22 > docs/characters.md, progression.md, product_brief.md, and README.md > a character compendium and a README classes section > Creates `docs/characters.md` as the single home for the identity layer — families, species, and the five-class roster, with skills/abilities to land there once designed — while `progression.md` keeps the mechanics and now links to the roster instead of duplicating it (also retiring the stale `Warden`/`Striker` example names). The README gains a Classes table so the MMO fantasy is visible to a first-time reader, plus a current-state line for XP/levels/stats, and the product brief links to the compendium.**

- **2026-07-21 > Sources/Worklings/PetCareCardView.swift and docs/pet_interaction.md > a Stats tab on the care card > Splits the care card into Condition and Stats tabs via a segmented control, adding actual XP numbers (not just a bar) and a five-stat readout with the active class's signature stat starred — room for abilities and gear later.**

- **2026-07-21 > Sources/Worklings/AppDelegate.swift and docs/pet_brain.md > a scripted activity rehearsal and hardcoded-name fixes > Adds "Run a Full Day, Sped Up" to the debug Simulate Activity menu: a six-step scripted day (dailyWake, an 11-simulated-minute focus session, workLogged, taskCompleted, milestone) paced visibly apart, with every timestamp anchored backward from kickoff so the pet's `lastUpdatedAt` never lands in the future and freezes real-time decay. Fixes the paw menu's Pet/Sleep/Roam items and header always reading "Pixel" regardless of the pet's actual name.**

- **2026-07-21 > Sources/CompanionCore/PetProgression.swift, PetState.swift, PetBrain.swift, ActivityEvent.swift, Sources/Worklings/PetSession.swift, AppDelegate.swift, PetCareCardView.swift, Tests/CompanionCoreChecks/ProgressionChecks.swift, and docs/progression.md, pet_brain.md > XP, levels, class, and stat growth > Implements the first progression-layer slice: cumulative XP (level always derived from it, never stored, so the two can never desync), five class-weighted stats that grow automatically on level-up with no banked/manually-spent currency, and a five-class roster (Wellspring, Juggernaut, Aegis, Maverick, Tinkerer) freely reassignable like family. XP is granted by dailyWake, care actions, Log Work, and completed Focus Sessions past a minimum qualifying duration, each gated by its own daily cap plus an overall daily cap and the existing condition multiplier — taskCompleted/milestone get a formula now but stay dormant until a real adapter exists. Adds a "Choose Class" menu mirroring family selection and a level/class/XP-progress row to the care card.**

- **2026-07-21 > docs/progression.md > the character/class design direction > Reframes Worklings as an avatar/character, not just a pet, and records the full systems ladder (level, stats, class, abilities, gear, dungeons, endgame, PVP) in build order. Replaces banked, manually-spent stat points with automatic class-weighted stat growth so a future abilities currency never has to share a schema with stat growth, splits persisted base stats from a future computed effective-stats layer for gear, softens the fairness posture for PVE-only cheating while keeping PVP deferred behind multiplayer normalization, and locks in the five-class roster (Wellspring, Juggernaut, Aegis, Maverick, Tinkerer), each a modern-culture term that also carries its own mythic weight.**

- **2026-07-21 > README.md and docs/distribution.md > v0.1.0-alpha.3 release status > Hyperlinks the README's changelog mention to `docs/changelog.md`, and records the published `v0.1.0-alpha.3` GitHub prerelease (build number 3) as bringing real activity awareness, Log Work, Focus Session, pet renaming, and the persistent name pill, bumping the packaging command examples to match.**

- **2026-07-21 > Sources/Worklings/WildkinPetView.swift and docs/pet_interaction.md > persistent name pill > Adds a small name pill below the pet that is always visible instead of only after opening the care card, hiding during the smoke transition like the rest of the pet and marked accessibility-hidden since the name is already spoken through the pet's accessibility label.**

- **2026-07-21 > Sources/CompanionCore/PetState.swift, Sources/Worklings/PetSession.swift, AppDelegate.swift, PetCareCardView.swift, Tests/CompanionCoreChecks/PetBrainChecks.swift, WorkLogChecks.swift, and docs/pet_brain.md > pet renaming > Adds `PetState.renamed(to:)`, trimming input and rejecting anything empty or over the new 24-character `maximumNameLength` by leaving the pet unchanged rather than accepting a bad value; wires it to a "Rename…" paw-menu alert and a pencil-icon inline editor next to the name in the care card, both gated the same way as every other action — the confirm control disables on an invalid draft rather than accepting and rejecting a submission.**

- **2026-07-21 > Sources/Worklings/PetSession.swift, AppDelegate.swift, PetCareCardView.swift, and docs/pet_brain.md > Focus Session, the first real workStarted/workEnded trigger > Adds a paw-menu item and care-card button that toggle between starting and ending a real work block, firing manual-tagged `workStarted`/`workEnded` events through the existing activity pipeline so Fullness and Energy drain at the same faster working rate already used for the simulated version, with no new CompanionCore behavior to test since the underlying event handling was already covered.**

- **2026-07-20 > Sources/CompanionCore/ActivityEvent.swift, ActivitySources.swift, PetBrain.swift, PetState.swift, PetPresentation.swift, Sources/Worklings/PetSession.swift, AppDelegate.swift, PetCareCardView.swift, Tests/CompanionCoreChecks/WorkLogChecks.swift, and docs/pet_brain.md > Log Work, the first self-reported activity source > Adds a `workLogged` event and `manual` source id, a paw-menu item and care-card button gated by `PetBrain.workLogAvailability` (a cooldown plus a hard daily cap, both checked before the action is even available, mirroring Feed's disabled-with-explanation pattern), and a fixed, non-adjustable Happiness gain so there is no user-chosen point value to game. Fixes two latent bugs surfaced by the new persisted fields: `updatedState` and `selectingFamily` now explicitly preserve them instead of silently resetting them on every tick or family swap.**

- **2026-07-20 > Sources/CompanionCore/ActivityEvent.swift and Tests/CompanionCoreChecks/ActivityChecks.swift > test suite audit > Removes the unused `SimulatedActivitySource.demoScript` and its check, and merges two overlapping away-trust-taper checks into one, after an audit of all 167 checks found no other redundancy.**

- **2026-07-20 > Sources/CompanionCore/ActivityEvent.swift, ActivitySources.swift, PetBrain.swift, Sources/Worklings/PetSession.swift, PresenceMonitor.swift, Tests/CompanionCoreChecks/ActivitySourceChecks.swift, ActivityChecks.swift, and docs/pet_brain.md > two-tier away-trust rate and a durable absence signal > Adds an `awaySince` timestamp to the activity context and a two-tier away-Trust rate (full rate for the first hour, a gentle trickle beyond it), fixing a gap where a genuine multi-hour absence silently stopped draining Trust after 30 minutes; the live presence monitor now keeps an ongoing absence alive by quietly re-touching the context without repeating its reaction, so the tiered rate sees the real elapsed time.**

- **2026-07-20 > Sources/CompanionCore/ActivitySources.swift, PetBrain.swift, PetState.swift, PetPresentation.swift, Sources/Worklings/PetSession.swift, AppDelegate.swift, PresenceMonitor.swift, Tests/CompanionCoreChecks/ActivitySourceChecks.swift, ActivityChecks.swift, and docs/pet_brain.md > real dailyWake, system presence detection, and full reaction coverage > Fires dailyWake for real on the first interaction of each calendar day, adds a permission-free system-idle presence monitor driving userIdle/userReturned, gives every activity event a visible reaction so structural events are no longer silent, and adds debug-only environment overrides (idle threshold, poll interval, and a per-hour rate multiplier) so manual testing does not require waiting on real clocks.**

- **2026-07-20 > Sources/CompanionCore/ActivityEvent.swift, PetBrain.swift, PetState.swift, PetPresentation.swift, Sources/Worklings/PetSession.swift, AppDelegate.swift, Tests/CompanionCoreChecks/ActivityChecks.swift, docs/pet_brain.md, and docs/architecture.md > normalized activity events, activity context, event reactions, and the simulated source > Implements the content-free activity-event pipeline: a nine-kind event vocabulary, a never-persisted context that expires to quiet, faster Fullness and Energy drain during work, Trust drain while the user is away that stops on return, celebratory and consoling reactions for share-worthy events including a Fullness/Energy cost for failure, a debug-only Simulate Activity menu with a live context readout, and behavioral checks for reduction, expiry, drain, reactions, clamping, and script determinism.**

- **2026-07-19 > docs/pet_brain.md, docs/pet_interaction.md, and docs/architecture.md > Fullness-first rewrite and progression alignment > Rewrites the Pet Brain and interaction docs to be shorter and player-facing, states every threshold and care effect in Fullness terms with a single internal-hunger note, frames needs as the condition layer, records the planned additive progression fields, and links the activity boundary to the progression design.**
- **2026-07-19 > docs/progression.md and README.md > progression design direction > Defines the two-layer condition/progression model, the normalized activity-event vocabulary and sources including daily wake and GitHub connect, XP with a condition multiplier and deferred caps, banked stat points per level, a battle-facing five-stat sheet with ambient effects, and caps-not-cryptography fairness principles.**

- **2026-07-18 > README.md, docs/distribution.md, docs/architecture.md, and docs/product_brief.md > v0.1.0-alpha.2 release status > Records the published `v0.1.0-alpha.2` GitHub prerelease as the first Worklings-branded DMG and narrows remaining distribution work to Developer ID signing and notarization.**
- **2026-07-17 > videos/worklings-teaser > storyboard, five HyperFrames compositions, local MusicGen soundtrack, SFX mix, assembled timeline, preview snapshots, and renders/worklings-teaser-15s.mp4 > Adds and renders a 15-second MMO-style companion-to-contender teaser using the three Workling families and existing first-party artwork; HyperFrames lint, headless runtime validation, accessibility checks, and layout inspection passed before rendering, while final MP4 review is intentionally left to the user.**
- **2026-07-17 > README.md, docs/architecture.md, docs/pet_interaction.md, docs/product_brief.md, and docs/distribution.md > shared smoke transition behavior and release status > Documents the non-persistent effect boundary, dense-frame swaps, launch/wake/tuck coverage, Reduce Motion fallback, verification expectations, and packaged resource.**
- **2026-07-17 > Sources/Worklings/CompanionPanelController.swift and Sources/Worklings/AppDelegate.swift > startTransition, show, hide, selectFamily, and transition-aware interaction guards > Plays smoke on launch, wake, tuck-away, and family replacement, pauses roaming and pointer interaction during the effect, and switches instantly when Reduce Motion is active.**
- **2026-07-17 > Sources/CompanionCore/PetPresentation.swift, Tests/CompanionCoreChecks/PetPresentationChecks.swift, Sources/Worklings/CompanionPanelController.swift, Sources/Worklings/WildkinPetView.swift, Package.swift, scripts/build_app_bundle, scripts/build_dmg, and scripts/verify_release > CompanionTransitionPlan, SmokeEffectSprite, transition presentation state, and resource packaging > Adds a tested eight-step reveal/conceal/swap contract, renders the shared smoke overlay, and requires the effect in development and release artifacts.**
- **2026-07-17 > assets/worklings-smoke-effects.png > eight-frame magical smoke transition > Adds a transparent shared pixel-art puff sequence that grows dense enough to obscure a Workling before dissipating.**
- **2026-07-17 > README.md, docs/architecture.md, docs/pet_brain.md, docs/pet_interaction.md, docs/product_brief.md, and docs/distribution.md > selectable-family behavior and packaging status > Documents three-family selection, state-preserving switches, backward-compatible persistence, shared rendering, release resources, and the remaining adoption boundary.**
- **2026-07-17 > Sources/Worklings/PetSession.swift and Sources/Worklings/AppDelegate.swift > PetSession.selectFamily and Choose Workling menu > Adds an immediate, checked three-family selector that persists the chosen appearance and exposes the active family in the menu header.**
- **2026-07-17 > Package.swift, Sources/Worklings/WildkinPetView.swift, Sources/Worklings/CompanionPanelController.swift, scripts/build_app_bundle, scripts/build_dmg, and scripts/verify_release > WorklingPetView, family sprite loading, and release resource checks > Generalizes the shared twelve-frame renderer and packages all three family sheets in repository, app-bundle, and DMG workflows.**
- **2026-07-17 > Sources/CompanionCore/PetState.swift, Sources/CompanionCore/PetBrain.swift, Tests/CompanionCoreChecks/PetBrainChecks.swift, and Tests/CompanionCoreChecks/PetPersistenceChecks.swift > PetFamily, PetState.selectingFamily, state propagation, and compatibility checks > Adds a persistent three-family identity while defaulting saves without the additive field to Wildkin and preserving needs, preferences, and simulation time when users switch.**
- **2026-07-17 > README.md, docs/architecture.md, docs/pet_interaction.md, and docs/product_brief.md > three-family runtime-art status > Records the shared twelve-frame contract across all three Workling families, keeps Wildkin as the only in-app mapping, and defers adoption and family selection.**
- **2026-07-17 > assets/worklings-relicborn-spritesheet.png > Relicborn idle, walking, mood, and care-reaction frames > Adds a transparent 4-by-3 keyback-pangolin runtime sheet with its wind-up key preserved across the shared twelve-frame pose contract.**
- **2026-07-17 > assets/worklings-elemental-spritesheet.png > Elemental idle, walking, mood, and care-reaction frames > Adds a transparent 4-by-3 ember-newt runtime sheet matching the shared twelve-frame Worklings pose contract.**
- **2026-07-17 > .github/workflows/ci.yml > build-and-check job > Builds every target and runs all CompanionCore behavioral checks on pull requests and main using a read-only pinned action.**
- **2026-07-17 > .github/PULL_REQUEST_TEMPLATE.md > pull request review template > Prompts contributors for the user outcome, tradeoffs, risk review, verification, documentation, and visual evidence.**
- **2026-07-17 > .github/ISSUE_TEMPLATE > bug, feature, and routing forms > Collects reproducible bug context, outcome-focused feature proposals, product-boundary impacts, and private security routing.**
- **2026-07-17 > .github/CODEOWNERS > repository ownership > Routes repository changes to the project owner until maintainership is delegated by area.**
- **2026-07-17 > CONTRIBUTING.md > contribution workflow and pull request acceptance > Defines issue-first changes, setup, architectural boundaries, testing expectations, review criteria, and licensing for external contributions.**
- **2026-07-17 > SECURITY.md > vulnerability reporting and supported versions > Adds a private reporting path, response expectations, and the local-first privacy boundary.**
- **2026-07-17 > CODE_OF_CONDUCT.md > community standards and enforcement > Adopts Contributor Covenant 2.1 with a confidential reporting route.**
- **2026-07-17 > README.md > documentation and contributing links > Links the contributor, security, and community governance policies from the repository entry point.**
- **2026-07-17 > Package.swift, Sources/Worklings/WildkinPetView.swift, Sources/Worklings/PlaceholderPetView.swift, and Sources/Worklings/CompanionPanelController.swift > Wildkin sprite loading, frame selection, and companion presentation > Replaces the code-drawn placeholder with mood-aware Wildkin frames, a reduced-motion-safe idle cycle, and care-reaction artwork while preserving the existing desktop interaction surfaces.**
- **2026-07-17 > scripts/build_app_bundle, scripts/build_dmg, scripts/verify_release, and docs/distribution.md > Wildkin runtime resource packaging and verification > Bundles the sprite sheet into packaged apps and makes release assembly and verification fail when the required pet artwork is missing.**
- **2026-07-17 > README.md, docs/pet_brain.md, docs/pet_interaction.md, docs/product_brief.md, and docs/architecture.md > Wildkin runtime-art status > Records the moss-fox as Pixel's first in-app character and narrows remaining artwork work to richer animation, additional families, and adoption.**
- **2026-07-17 > assets/worklings-wildkin-spritesheet.png > Wildkin idle, walking, mood, and care-reaction frames > Adds a transparent 4-by-3 pixel-art sprite sheet for replacing the code-drawn placeholder pet and supporting the first autonomous-movement slice.**
- **2026-07-17 > Sources/Worklings/CompanionPanelController.swift and CompanionHostingView.swift > roaming loop and drag lifecycle > Animates safe single-display movement while pausing for pointer interaction, care, dragging, tuck-away, and Reduce Motion, then clamps manual drag endpoints.**
- **2026-07-17 > Sources/Worklings/WildkinPetView.swift > walking frame selection and facing > Plays the four-frame Wildkin walk cycle and flips only the sprite to face its direction of travel.**
- **2026-07-17 > Sources/Worklings/AppDelegate.swift > roaming menu preference > Adds a persistent opt-in roaming control with explicit pause and Reduce Motion states.**
- **2026-07-17 > README.md, docs/architecture.md, docs/pet_brain.md, docs/pet_interaction.md, and docs/product_brief.md > safe idle-roaming behavior > Documents the opt-in contract, interruptions, ownership boundaries, implemented status, and deferred movement intelligence.**
- **2026-07-17 > Sources/CompanionCore/ScreenPlacement.swift > PetRoamingPlanner.intent and ScreenPlacement.roamingOrigin > Adds deterministic idle-roaming timing and screen-relative targets that clamp and reflect within one visible display.**
- **2026-07-17 > Tests/CompanionCoreChecks/ScreenPlacementChecks.swift > roaming planner and destination checks > Covers deterministic pattern repetition, scaled movement, bounds, and inward reflection at display edges.**
- **2026-07-17 > README.md > visual asset licensing > Clarifies that first-party concept art and code-rendered placeholder visuals share the repository license unless an asset states otherwise.**
- **2026-07-17 > Sources/CompanionCore/PetState.swift > PetNeeds.fullness > Restores the approved derived inverse of hunger after the Worklings rebrand without changing persistence or Pet Brain rules.**
- **2026-07-17 > Sources/Worklings/PetCareCardView.swift and Sources/Worklings/AppDelegate.swift > need presentation > Uses Fullness, Energy, Happiness, and Trust consistently so every higher value means better wellbeing.**
- **2026-07-17 > Tests/CompanionCoreChecks/PetBrainChecks.swift and Tests/CompanionCoreChecks/PetPersistenceChecks.swift > fullness checks > Covers fullness conversion boundaries and verifies the derived value does not enter the versioned save.**
- **2026-07-17 > docs/pet_brain.md and docs/pet_interaction.md > meter semantics > Documents the positive wellbeing display rule and the unchanged internal hunger model.**
- **2026-07-17 > docs/pet_brain.md > implemented Pet Brain reference > Replaces the completed vertical-slice plan with the current state model, exact care rules, tuning caveats, persistence behavior, verification coverage, and next domain work.**
- **2026-07-17 > docs/product_brief.md > validated experience and MVP progress > Separates implemented behavior from remaining scope and records Wildkin, Elemental, and Relicborn as MMO-style concept families rather than shipped choices.**
- **2026-07-17 > docs/architecture.md > implemented topology and delivery status > Documents the current Swift targets, live session, rebrand compatibility, packaging path, planned adapter boundary, artwork boundary, and remaining slices.**
- **2026-07-17 > docs/pet_interaction.md > implementation status and art boundary > Aligns interaction guidance with the shipped hover and care-card behavior while distinguishing Pixel's placeholder from concept artwork.**
- **2026-07-17 > docs/distribution.md > Worklings release transition > Records the historical Build Companion prerelease, the pending Worklings artifact, updated example version, and safe first rebrand upgrade.**
- **2026-07-17 > README.md > current implementation and release status > Updates the public overview for positive meters, legacy-save copy, packaging tools, concept-only artwork, and the pending Worklings-branded download.**
- **2026-07-17 > README.md, assets/worklings.png, and assets/worklings_interaction.png > placeholder project visuals > Removes the superseded purple pet portrait and care-card screenshot along with the remaining README reference.**
- **2026-07-17 > README.md and assets/worklings-social-preview.jpg, assets/worklings-wildkin.png, assets/worklings-elemental.png, and assets/worklings-relicborn.png > project banner and character direction > Adds the approved Worklings concept banner and Wildkin, Elemental, and Relicborn artwork to the repository overview.**
- **2026-07-17 > README.md and assets/worklings.png and assets/worklings_interaction.png > project visuals > Adds the supplied Worklings portrait and care-card screenshot to the project overview.**
- **2026-07-17 > Package.swift, Sources/Worklings, scripts/build_app_bundle, scripts/build_dmg, and scripts/verify_release > package, executable, app entry point, bundle metadata, and release artifact configuration > Renames the macOS app and technical build identity from Build Companion to Worklings.**
- **2026-07-17 > Sources/Worklings/PetSession.swift > makeDefaultStore > Stores saves under Application Support/Worklings and copies forward any legacy BuildCompanion save without deleting the original.**
- **2026-07-17 > README.md, docs/architecture.md, docs/distribution.md, docs/pet_brain.md, and docs/product_brief.md > product naming and positioning > Establishes Worklings as the canonical project name and clarifies its MMO-inspired persistent-pet experience.**

- **2026-07-16 > scripts/verify_release > mounted artifact verification > Confirms the DMG checksum and integrity, mounts it read-only, and validates its Applications shortcut, bundle metadata, architecture, and code signature.**
- **2026-07-16 > README.md > beta download and installation > Documents release availability, checksum verification, drag-to-install, Apple Silicon support, and safe first launch of an ad-hoc-signed alpha.**
- **2026-07-16 > docs/distribution.md > release verification command > Documents the final mounted-artifact verification gate.**
- **2026-07-16 > scripts/build_dmg > DMG assembly and checksum generation > Validates a versioned signed app, packages it with an Applications shortcut, verifies the compressed image, writes a SHA-256 checksum, and refuses artifact overwrites.**
- **2026-07-16 > docs/distribution.md > DMG build command > Documents how the application bundle becomes a verified disk image and checksum.**
- **2026-07-16 > scripts/build_app_bundle > release build and bundle assembly > Builds a versioned architecture-specific Swift release, writes validated macOS metadata, installs the executable, applies an ad-hoc signature, and refuses to overwrite an existing app.**
- **2026-07-16 > .gitignore > release artifact exclusions > Excludes the generated `dist/` tree from version control.**
- **2026-07-16 > docs/distribution.md > versioned output and app build command > Aligns the artifact contract with isolated version directories and documents the bundle builder.**
- **2026-07-16 > docs/distribution.md > beta artifact and release contract > Defines the initial Apple Silicon app bundle, ad-hoc signing, DMG layout, versioning, Gatekeeper guidance, verification gates, and GitHub prerelease process.**
- **2026-07-15 > README.md > project introduction > Adds the project author's motivation, Codex-first direction, open context-awareness question, and note about AI-assisted writing.**
- **2026-07-15 > docs/git_workflow.md > pull request guidance > Requires concise, review-focused pull requests and excludes commit narration, redundant roadmap content, and generic boilerplate.**
- **2026-07-15 > README.md > contributing section > Welcomes focused pull requests and defines build, check, coverage, changelog, documentation, privacy, accessibility, save compatibility, review evidence, and licensing expectations.**
- **2026-07-15 > LICENSE > Apache License 2.0 terms > Adds the complete permissive project license with redistribution conditions and contributor patent grant.**
- **2026-07-15 > README.md > license section > Summarizes Apache-2.0 usage and clarifies that future artwork may declare separate asset-specific terms.**
- **2026-07-15 > README.md > project overview, source usage, verification, and beta distribution > Adds the public project entry point with product direction, current capabilities, local run instructions, checks, documentation links, and planned GitHub Release DMGs.**
- **2026-07-15 > AGENTS.md > documentation location rule > Allows standard repository entry-point and governance files at the root while keeping detailed project documentation under `docs/`.**
- **2026-07-15 > Sources/Worklings/CompanionHostingView.swift > mouse tracking, click classification, and dragging > Gives AppKit ownership of hover, click, and drag behavior so moving Pixel cannot open the care card.**
- **2026-07-15 > Sources/Worklings/HoverSummaryPanelController.swift > delayed summary presentation and edge placement > Adds a styled click-through hover summary that stays inside the active display.**
- **2026-07-15 > Sources/Worklings/CarePopoverController.swift > toggle, show, and close > Adds a transient pet-anchored interactive popover with native dismissal behavior.**
- **2026-07-15 > Sources/Worklings/PetCareCardView.swift > need rows, care actions, and preferences > Adds exact need progress, shared action availability, selectable favourites, reaction feedback, and accessibility labels.**
- **2026-07-15 > Sources/Worklings/PetSession.swift > careStatus and guarded perform > Exposes shared care status and prevents unavailable care actions from bypassing interface state.**
- **2026-07-15 > Sources/Worklings/CompanionPanelController.swift > pointer, hover, and care-card coordination > Replaces background dragging and SwiftUI taps with delayed hover and click-versus-drag AppKit coordination.**
- **2026-07-15 > Sources/Worklings/PlaceholderPetView.swift > interaction ownership and accessibility hint > Removes click-to-pet and exposes the natural-language care summary to accessibility technologies.**
- **2026-07-15 > Sources/Worklings/AppDelegate.swift > shared care availability > Keeps menu-bar Feed, Play, and Sleep availability aligned with the pet care card and PetSession guardrails.**
- **2026-07-15 > Sources/CompanionCore/PetState.swift > PetNeeds decoding and clamping > Routes decoded need values through finite `0...100` domain clamping.**
- **2026-07-15 > Sources/CompanionCore/PetCareStatus.swift > urgency, summaries, and action availability > Centralizes need thresholds, priority ordering, two-condition hover language, ambient alerts, and care-action rules.**
- **2026-07-15 > Sources/CompanionCore/PetPresentation.swift > happy mood presentation > Suppresses persistent happy thought bubbles in line with the interaction feedback policy.**
- **2026-07-15 > Tests/CompanionCoreChecks/PetCareStatusChecks.swift > care status checks > Covers urgency boundaries, condition ranking, summary limits, ambient alerts, and action availability.**
- **2026-07-15 > Tests/CompanionCoreChecks/PetPersistenceChecks.swift > decoded clamping check > Verifies valid JSON cannot restore need values outside the domain range.**
- **2026-07-15 > Tests/CompanionCoreChecks/PetPresentationChecks.swift > quiet happy presentation check > Verifies content pets do not emit persistent positive thought bubbles.**
- **2026-07-15 > Tests/CompanionCoreChecks/CheckRunner.swift > care status suite registration > Adds the urgency and care-availability suite to the dependency-free check run.**
- **2026-07-15 > docs/pet_interaction.md > interaction hierarchy, urgency, care actions, and verification > Defines ambient, hover, click-card, menu-bar, pointer, accessibility, and testing behavior before movement work begins.**
- **2026-07-15 > Sources/CompanionCore/PetState.swift > PetFood, PetPlayActivity, and PetMood conformance > Makes semantic pet enums explicitly equatable for reusable behavioral checks.**
- **2026-07-15 > Tests/CompanionCoreChecks/CheckContext.swift > expectations and failure collection > Adds dependency-free equality, approximation, and error assertions shared across core checks.**
- **2026-07-15 > Tests/CompanionCoreChecks/CheckRunner.swift > main > Runs placement, Pet Brain, persistence, and presentation check suites with one failure summary.**
- **2026-07-15 > Tests/CompanionCoreChecks/ScreenPlacementChecks.swift > run and placement cases > Migrates the existing placement coverage into the shared multi-suite runner.**
- **2026-07-15 > Tests/CompanionCoreChecks/PetBrainChecks.swift > Pet Brain behavior checks > Covers clamping, defaults, mood priority, deterministic clocks, offline limits, preferences, refusal, and sleep tradeoffs.**
- **2026-07-15 > Tests/CompanionCoreChecks/PetPersistenceChecks.swift > JSON store checks > Covers missing files, state round trips, unsupported schemas, and corrupt-save preservation.**
- **2026-07-15 > Tests/CompanionCoreChecks/PetPresentationChecks.swift > placeholder presentation checks > Covers content, urgent needs, reaction overrides, and tired-refusal placeholder output.**
- **2026-07-15 > Sources/CompanionCore/PetPresentation.swift > make and reaction thought mapping > Converts semantic pet mood and interactions into testable placeholder face, palette, label, and thought content.**
- **2026-07-15 > Sources/Worklings/PetSession.swift > advance, perform, persist, and reaction scheduling > Connects the Pet Brain to a live session, preserves corrupt saves, stores state in Application Support, and clears temporary reactions.**
- **2026-07-15 > Sources/Worklings/AppDelegate.swift > menuNeedsUpdate and care actions > Adds live need summaries, favourite markers, and feed, play, pet, and sleep controls to the menu-bar interface.**
- **2026-07-15 > Sources/Worklings/CompanionPanelController.swift > init and configurePanel > Injects the live pet session into a larger placeholder panel that can display thought content.**
- **2026-07-15 > Sources/Worklings/PlaceholderPetView.swift > mood presentation and tap interaction > Adds mood palettes, expressive faces, thought bubbles, and click-to-pet feedback to the temporary companion.**
- **2026-07-15 > Sources/CompanionCore/PetState.swift > PetNeeds, PetState, mood, actions, and reactions > Defines clamped versioned pet state, preferences, semantic moods, and care interaction types.**
- **2026-07-15 > Sources/CompanionCore/PetBrain.swift > advance and perform > Adds deterministic need progression, offline clamping, distress penalties, and preference-aware care outcomes.**
- **2026-07-15 > Sources/CompanionCore/PetStateFileStore.swift > load and save > Adds version-checked JSON loading and atomic local persistence without overwriting unreadable saves.**
- **2026-07-15 > docs/pet_brain.md > Pet Brain vertical-slice plan > Defines needs, time progression, care interactions, persistence, placeholder presentation, test coverage, and review criteria for the next four commits.**
- **2026-07-15 > Package.swift > package manifest > Adds a dependency-free macOS Swift package with application, core library, and executable check targets.**
- **2026-07-15 > Sources/CompanionCore/ScreenPlacement.swift > defaultOrigin and clampedOrigin > Calculates safe companion-window positions within the usable screen frame.**
- **2026-07-15 > Sources/Worklings/WorklingsApp.swift > main > Starts Worklings as a menu-bar macOS application.**
- **2026-07-15 > Sources/Worklings/AppDelegate.swift > applicationDidFinishLaunching, toggleCompanionVisibility, and quit > Manages the companion lifecycle and menu-bar controls.**
- **2026-07-15 > Sources/Worklings/CompanionPanelController.swift > configurePanel, placeOnMainScreen, show, and hide > Creates a transparent draggable floating panel and positions it safely on the main display.**
- **2026-07-15 > Sources/Worklings/PlaceholderPetView.swift > body, ears, and face > Draws an animated placeholder companion that respects reduced-motion settings.**
- **2026-07-15 > Tests/CompanionCoreChecks/ScreenPlacementChecks.swift > screen placement checks > Covers default, clamped, and oversized-window placement without requiring a bundled test framework.**
- **2026-07-15 > scripts/committer > repository and branch detection > Supports the first commit on an unborn branch and refuses detached-HEAD commits.**
- **2026-07-15 > docs/git_workflow.md > branching, committing, pushing, and releases > Moves the Git workflow into the documentation folder and aligns it with the actual committer safeguards.**
- **2026-07-15 > docs/product_brief.md > product vision, MVP scope, and success criteria > Defines the macOS-first companion experiment, provider-neutral direction, privacy boundary, and reversible neglect model.**
- **2026-07-15 > docs/architecture.md > system boundaries and component decisions > Defines the native Swift architecture, activity adapter contract, deterministic simulation, local persistence, and delivery slices.**
- **2026-07-15 > .gitignore > macOS and Swift build exclusions > Prevents local metadata, build products, Xcode user state, and packaged disk images from entering version control.**
